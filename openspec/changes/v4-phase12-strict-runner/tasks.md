## 1. Planning artifacts (Phase 1 charter)

- [x] 1.1 Create change `v4-phase12-strict-runner` with `skip_specs: true` and author `proposal.md` (scope phase 1 to characterization/contract mapping only; prohibit pipeline behavioral/config/model-routing/fidelity/refactoring without later approval) — verify: `openspec validate v4-phase12-strict-runner --json` passes with `skip_specs` info.
- [x] 1.2 Author `design.md` restating current `main` state (4987-line runner, whole-chapter + chunk paths, journal/resume, B3 audit/repair/formatting, artifacts, identity) and Phase 1 No-Go guardrails — verify: `design.md` present, no code diff.
- [x] 1.3 Author `contract-map.md` covering all required surfaces (see Section 2) — verify: `contract-map.md` present, grep-referenced line numbers match `v4_phase12_strict_runner.py` at commit `HEAD`.
- [x] 1.4 Enforce planning-only hygiene: no edits to `pact_v4/pipeline/v4_phase12_strict_runner.py` or callers; `git diff --stat origin/main...HEAD` shows only `openspec/changes/v4-phase12-strict-runner/**` and `.openspec.yaml` — verify: `bash .pi/skills/pact-workspace-guard/scripts/check.sh` PASS, `bash .pi/skills/pact-git-hygiene/scripts/check.sh` PASS, `git status --porcelain` clean except docs.

## 2. Contract map (read-only inventory)

- [x] 2.1 Journal/Resume — document `_load_journal`, journal NDJSON schema `pact-v4-strict-chapter-trial-journal/v2`, append+flush per entry, resume replay (`prior_entries`, `resumed_from_index`), foreign-identity checks (`chunk_plan_hash`, `config_identity`, `backend_identity_hashes`), `StrictChapterRunResult.resumed_from_index`, and `whole_chapter` single-entry invariant (`WHOLE_CHAPTER_CHUNK_ID`) — verify: references `v4_phase12_strict_runner.py:754`, `:2690`, `:4167`, `:3696`, plus test `test_v4_phase12_strict_runner_whole_chapter.py`.
- [x] 2.2 Whole-chapter vs Chunk flows — enumerate branching in `run_chapter_strict` (`if cfg.whole_chapter: return _run_whole_chapter_strict`), `ChunkPlanArtifact.create` + `mode=whole-chapter-derived`, `WholeChapterPidMap.derive`, per-chunk loop (`_left_ru_for_chunk`, `_risk_for_chunk`, cascade `select_candidate`, `PhaseProgressWriter.chunk_started/done`) vs single `generate_whole_chapter` + `_validate_whole_chapter_generation_record` + `wc_*` progress events — verify: `_shared_runner_helpers.py` helpers listed, `b3` still chunked even in WC mode.
- [x] 2.3 Audit/Repair/Formatting — map Step 6 (`_run_step6_audit` → `run_chapter_audit` iterates `chunk_plan.chunks`, `audit_cache_b3.json`, `b2_handoff.json`), Step 7 (`_run_step7_repair` → repair batches, `repair_cache.json`, `translations_repaired.json`, `re-audit` chunked), Phase 5 (`formatting_report.json` → `translations_final.json`/`translations.json` terminal), and `run_audit` gating identity impact — verify: `b3_audit_repair.py` + `pact_v4/audit/*.py` referenced, `cfg.run_audit` identity note in `to_config_artifact`.
- [x] 2.4 Persistent artifacts — inventory all `cfg.out_dir` files with schema/version and writer/reader: `chunk_plan.json`, `whole_chapter_pid_map.json`, `journal.ndjson`, `generation_outcomes.json`, `selection_meta.json`, `selection_results.json`, `translations_raw.json`, `translations.json` (+ `translations_repaired.json`, `translations_final.json`), `strict_chapter_trial_record.json`, `audit_cache_b3.json`/`audit_findings`, `b2_handoff.json`, `repair_cache.json`/`repair_report.json`, `formatting_report.json`, `quarantined_retry.json`, `phase_progress.ndjson` (write-only), `usage.ndjson`, `whole_chapter_reasoning.txt` — verify: helpers `_audit_cache_path`, `_generation_outcomes_path`, etc. listed with line numbers.
- [x] 2.5 Identity/Determinism — enumerate `StrictRunConfig` knobs that participate in `to_config_artifact` / `config_identity`, `build_source_artifact` / `snapshot` / `chunk_plan_hash` / `backend_identity_hash` / `candidate_ids` / gate trace provenance, deterministic selection (`select_candidate`), left_context hash, `phase_progress` diagnostics-only contract, and `Data loss`/`Foreign identity` error taxonomy — verify: `StrictRunConfig:269` + `to_config_artifact:483` + `JournalEntry:692` referenced, `DECISIONS.md` whole-chapter identity note.
- [x] 2.6 Callers/Tests — list production callers (`pact_full_pipeline_runner_v1/v4_phase12_strict_run.py:934`, `v4_book_run.py:165`), monitor consumers (`v4_phase_progress.py:6`, `v4_usage.py:6`), and test suites (`tests/pact_v4/pipeline/test_v4_phase12_strict_runner*.py` × 8, `test_v4_phase_progress*`, `test_v4_usage_record`, `_smoke_c3.py`, `test_bm_book_memory_integration.py`, `test_a2_prompt_and_snapshots.py`) with per-suite focus — verify: `grep -rn run_chapter_strict` counts captured in contract-map.

## 3. Candidate pure-internal extractions (identified, not implemented)

- [x] 3.1 Enumerate candidates that are pure helpers with **no** pipeline-visible effect if extracted verbatim (e.g., `_atomic_write_json`, `_left_context_hash`, `_pid_diffs`, `_gates_passed`, `_pick_best_variant`, `_persist_whole_chapter_reasoning`, `_validate_whole_chapter_generation_record`, `_normalize_final_markup`, `_build_phase4_provenance`, `_load_journal`/`_close_run_resources` helpers) — mark each `Status: Not Implemented — requires follow-up change` — verify: list in `contract-map.md` Table 3 with purity rationale and why it touches no identity.
- [x] 3.2 Explicitly mark **prohibited** under this change: any move/rename, signature change, behavioral/config/model-routing/fidelity change, or refactoring of the above candidates — verify: `proposal.md` What Changes + `design.md` Decisions + `contract-map.md` §7 state prohibition and "later approval required".

## 4. Validation (planning-only)

- [x] 4.1 `openspec validate --changes --json` shows `v4-phase12-strict-runner` PASS (`skip_specs` info) and `v41-runtime-efficiency` PASS — verify: paste JSON output in commit message / PR body.
- [x] 4.2 Guard checks: `bash .pi/skills/pact-workspace-guard/scripts/check.sh` PASS, `bash .pi/skills/pact-risk-test/scripts/check.sh` Low (no code diff), `bash .pi/skills/pact-git-hygiene/scripts/check.sh` PASS, `git diff --name-only origin/main...HEAD` only docs — verify: logs attached.
- [x] 4.3 Commit planning artifacts only on `openspec/strict-runner-contract-map` worktree and push — verify: `git log --oneline -1` shows docs commit, `git status` clean.

## 5. Out of scope (explicit)

- Any implementation of extractions, file splits, or runner modularization — deferred to phase 2 change after owner approval.
- Pipeline behavioral, config/model-routing/fidelity, prompt, glossary, audit, or formatting changes.
- Architecture/product decisions beyond the planning charter.
