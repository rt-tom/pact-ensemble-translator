## 1. Store skeleton and manifest schema

- [x] 1.1 Implement `pact_v4/snapshot/store.py` with configurable root (default `/home/rt/pact_runs/books/<book-id>/`), path resolvers for `CURRENT.json`, `locks/`, `incoming/`, `_bootstrap_inbox/`, `quarantine/`, `snapshots/<revision-id>/`, and atomic temp-write + rename helpers — verify: unit test creates skeleton, resolves paths, and performs atomic rename without network/model/pipeline.
- [x] 1.2 Implement `pact_v4/snapshot/manifest.py` with the Scope A schema (schema_version, book_id, revision_id, parent_revision_id, created_at, published_at, terminal_status ∈ {bootstrap-seed, complete, accepted_degraded}, tool_version, source{path_on_rt, operator, host}, state_files[{rel_path, sha256, size}], excludes[], code_commit), SHA-256/size computation, serialization, and validation that rejects secrets/environment/server/model-cache fields — verify: schema round-trip and a test asserting secret fields are excluded and bad terminal_status is rejected.
- [x] 1.3 Implement `pact_v4/snapshot/errors.py` with `SnapshotError` hierarchy (`ValidationError`, `LeaseHeld`, `StaleParent`, `HashMismatch`) — verify: tests assert each error type is raised at the correct protocol step.

## 2. Bootstrap and promotion commands

- [x] 2.1 Implement `pact_v4/snapshot/bootstrap.py`: read `_bootstrap_inbox/<ts>/`, select exactly the four canonical state files, validate JSON, build `rev-0001` bootstrap-seed manifest (parent null), write `snapshots/rev-0001/state/` + `manifest.json`, atomically write `CURRENT.json` — verify: bootstrap from a synthetic inbox produces rev-0001 + CURRENT; non-JSON input fails closed without creating/advancing CURRENT.
- [x] 2.2 Implement `pact_v4/snapshot/lease.py`: atomic CAS acquire of `locks/<book-id>.lease.json` bound to parent revision, release (delete), and append-only JSONL audit in `locks/<book-id>.lease_audit.jsonl` — verify: acquire/release round-trip; existing lease causes `LeaseHeld`; `--check-expired` reports without releasing.
- [x] 2.3 Implement `pact_v4/snapshot/promote.py`: validate manifest + state-file hashes/sizes, require eligible terminal_status, require `parent_revision_id == current` (fail-closed `StaleParent`), acquire lease mutex, atomically move bundle to new `snapshots/<revision-id>/`, write `CURRENT.json` via atomic rename only while lease still references same parent, release lease, return ACCEPTED; on any failure move candidate to `quarantine/<candidate-id>/`, return `REJECTED` with reason and non-zero exit — verify: accept path advances CURRENT + releases lease; stale-parent, hash-mismatch, and held-lease paths quarantine and preserve prior CURRENT.
- [x] 2.4 Implement `pact_v4/snapshot/cli.py` with subcommands `init-store`, `bootstrap`, `promote`, `release-lease`; JSON verdicts on stdout; exit 0 accept / non-zero reject; `--root` override — verify: each subcommand exercised in tests; `release-lease --check-expired` is read-only.

## 3. Documentation and verification

- [x] 3.1 Document the owner bootstrap procedure (manual copy of `pact_chapters` into `_bootstrap_inbox/<ts>/`, then `pact-bootstrap`) and the SSH/SFTP restricted `command=` snippet in README/operator notes; state that key generation and `authorized_keys` edits are owner ops outside the repo and that no SSH/SFTP client is implemented in this slice — verify: doc reviewed for no executable production-run claim.
- [x] 3.2 Run narrow offline tests (`tests/pact_v4/snapshot/`), `git diff --check`, and `openspec validate book-state-snapshot-handoff-impl --strict`; verify no network, model-server, pipeline execution, or RT production-path edit occurred; independent `pact-rev` review passes before merge.
