# V4 — Монитор book_run (несколько глав) — карточка-план (TODO, не реализация)

- Дата: 2026-08-06
- Статус: **план, НЕ реализация** (владелец: «пока только в план, не в реализацию»)
- Проблема: `v4_phase_progress` мониторит ОДИН out-dir главы
  (`--out-dir ... --watch`). `v4_book_run` (несколько глав) пишет артефакты в
  подпапки `--out-base/chapter_<id>/` (phase_progress.ndjson каждой главы) —
  единого монитора book_run нет (при первом боевом book-run 0001+0002
  потребовались два окна монитора).

## Что сделать (когда дойдём)

- Расширить `pact_full_pipeline_runner_v1/v4_phase_progress.py` (или отдельный
  CLI `v4_book_progress`) режимом `--out-base`:
  - найти подпапки `chapter_*/` с `phase_progress.ndjson` (+ journal.ndjson,
    b2_handoff.json, repair_report.json);
  - агрегировать: список глав с прогрессом (не начата / шаг N / завершена,
    terminal-статус), текущая активная глава, общие счётчики (chunks
    selected/quarantined, budget usage из usage.ndjson если есть), время;
  - `--watch N` — как в существующем мониторе; read-only (не трогать
    артефакты/пайплайн);
  - главы добавляются динамически (book_run идёт по порядку — вторая глава
    появляется после первой).

## НЕ входит

- Изменения пайплайна/book_run/артефактов; только read-only диагностика.
- Прогоны и запуск — только владельцем вне чата (правило 2026-08-06).

## Связанное

- `docs/plans/V4_PHASE12_RUN_PROGRESS_TRACKER_TASK_RU.md` (монитор одиночной главы).
- Первый боевой book-run: `v4_book_0001-0002_remote`.
