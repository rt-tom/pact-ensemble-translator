2026-08-06. **V4 Efficiency A1 (ред.2) — glossary budgeter + prompt prefix ordering + D1-telemetry (реализация, ветка vk/v4-efficiency-a1).** A1.1: per-chunk glossary budget — пара из glossary.json попадает в PromptBundle.glossary только если source-термин присутствует в owned+left+right (_term_present из risk.py, IGNORECASE, word-boundary, мультисловные) или always_include fail-closed: narrator_gender-имя рассказчика (pov.source_name, включая мультисловные пары), glossary_conflict-несущие (source-термин с >1 предписанной формой), записи категорий required_risk_feature_codes чанка (number_word/tone_profanity, те же замороженные regex). Диагностика: glossary_budget_report.json (отброшено N пар: [термины]), bundle_hash от отфильтрованного набора (чанк без фильтрации сохраняет хеш — resume не инвалидируется лишне). Библия НЕ режется, audit skip / bundle dedup / Qwen re-gate bypass НЕ вводятся (non-goals ред.2). A1.2: статичные блоки (template instructions + полная библия + style/policy) в начало generation/Qwen fidelity/Qwen audit/Gemma audit промптов, динамические после — содержимое не меняется, общий префикс для cached_input_tokens (проверка после прогона по D1). A1.3: v4_usage добавил разрез по фазам (gen/qwen_fidelity/gemma_preference/audit/repair/formatting, cached/reasoning токены) из usage.ndjson — read-only. Dry-run на run_005 (генерация, 16 чанков/32 вызова): отброшено 1779 пар, input ~ -16.5% (len/4-оценка) — отчёт docs/audits/V4_EFFICIENCY_A1_RUN005_DRY_RUN.json. Quarantined-retry regeneration (B6, phase4/quarantined_retry.py) вне A1-скоупа — остаётся на полной библии glossary. Ожидание после прогона: gen input -10-15% + кэш-выигрыш A1.2.
2026-08-06. **V4 Efficiency A — ред. 2 (решения владельца после разбора архитектором).** (1) Библия НЕ режется (ни по главам, ни по ролям) — эвристика релевантности ненадёжна; Bible budgeter исключён. (2) A2 — делаем: один кандидат balanced_literary по умолчанию, lazy fidelity_first только при fail balanced; Gemma preference 13→0-2. (3) Исключены из A1: audit skip (Step 6 ловит ты/вы/referent — deterministic не видит), bundle dedup по bundle_hash (identity включает chunk_id/контекст), blanket Qwen re-gate bypass. (4) A–D: A (compact-форматы) и B (adaptive max_output_tokens) — в A1b после замера; C (repair через качество входа) — в A1b; D (content-addressable cache) — отклонён (resume-кэш уже покрывает; хеш без chunk_id небезопасен). A1 (ред.2): glossary budgeter + prompt prefix ordering (provider cache) + D1-telemetry. Ожидание: 404→~365 вызовов (-10%), input -10-13% + кэш; A1b — отдельной карточкой после прогона. План: docs/plans/V4_EFFICIENCY_A_OPTIMIZATION_PLAN_RU.md (ред. 2), карточки A1/A2.
2026-08-06. **V5 §8.1 уточнение + V4 Efficiency A — детерминированная оптимизация вызовов/токенов (план, исполнение архитектором).** V5 §8.1 : characters/aliases/pronouns и термины — deterministic-first (frequency ≥2 глав + regex + consensus alignment PID→PID + co-occurrence guard как B9, consensus 0.8); LLM только для ambiguous clusters (the fat man=ghoul, Mags≠Maggie). Phase 6: ~3 LLM-вызова на книгу вместо ~10 (внесено в docs/architecture/V5_UNIVERSAL_LITERARY_TRANSLATOR_ARCHITECTURE_RU.md:8.1). V4 план Efficiency A: A1 prompt budgeter (glossary/bible по релевантности чанку, locked never cut: narrator_gender/glossary_conflict/number_word/tone_profanity) + audit/repair/formatting deterministic skip + bundle dedup; A2 lazy balanced-only (default 1 balanced_literary, fidelity_first только lazy при fail Qwen/deterministic, Gemma только при оба passed+disagreement) — run_005 404→~250-270 вызовов (-33-38%), input -33%, без reasoning/topology и без потери по литобзору (overthinking на low, minimal repair, MBR 2310.06707). Детальный план: docs/plans/V4_EFFICIENCY_A_OPTIMIZATION_PLAN_RU.md. Исполнение: архитектор → карточки A1/A2 (vk/v4-efficiency-a1/a2) по очереди, A1 до A2, прогоны 0001+046 только владельцем вне чата (AGENTS.md 2026-08-06).
2026-08-06. **D1-fix (замечания ревью PR #137, HIGH-1/HIGH-2): crash-safe resume framing + durable dedup в usage.ndjson.** HIGH-1: после crash внутри фазы файл мог кончаться незавершённой строкой — свежий writer при resume дописывал первую запись в ту же физическую строку, читатель пропускал объединённую строку и терял первую запись после resume. Фикс: writer перед первым resumed-append изолирует хвост (дописывает newline, завершённые записи не переписываются). HIGH-2: writer безусловно дописывал — повторный push того же session/request id давал неразличимые дубли. Фикс: durable dedup по identity (session_id+request_id; записи без identity всегда пишутся — distinct calls сохраняются). Writer остаётся диагностикой: сбой записи по-прежнему только отключает артефакт.
2026-08-06. **B9-офлайн-валидация авто-промоута (dry-run на артефактах run_005, глава 0001) — итоги.** 0 модельных вызовов (детерминированный dry-run, тест-харнесс test_b9_offline_validation.py, temp-копии glossary/book_memory — production НЕ тронут; отчёт: docs/audits/B9_OFFLINE_VALIDATION_RUN005_RU.md, PR #136). Результаты: 273 кандидата (6 proper_name, 267 term), proposed 6 (все proper_name), conflicts 186 (co-occurrence guard работает), term-промоут не сработал (нужна 2-я глава, term_min_chapters=2). **Найденные дефекты: HIGH 1 — «Master» (титул-обращение) → «Блэйк» (совстречающееся имя; proper_name-выравнивание берёт капитализированное слово без проверки, что это перевод); HIGH 2 — term-коллокации advantage→получить, anger→чувствовал, blonde→стороны (share-метрика игнорирует истинный перевод, присутствующий в 2-3 из 3 pids; в book-run со 2-й главой закоммитилось бы в glossary); MEDIUM 3 — топоним «Jacob's Bell» → «Якобс» (фрагмент составного имени); LOW 4 — target в косвенном падеже (Крисом, Якобс; должен быть именительный).** **ВЕРДИКТ: B9 НЕ готов к боевому book-run** — авто-промоут затронул бы production glossary.json мусорными парами. **Решение владельца: B9-fix обязателен до book-run** (карточка t_800fedaf: ужесточение эвристик/порогов, регрессии на 4 пары).
2026-08-06. **D1: per-call remote usage пишется из backend-синка, не из фазовых границ.** Card D1 требует usage.ndjson «на каждый завершённый remote-вызов» с crash-safety внутри фазы. Выбор точки записи (открытый вопрос карточки): не runner-drain по фазам (теряет вызовы при crash внутри фазы) и не coordinator events_since (ленивая materialization), а sink в OpenCodeServerBackend, вызываемый при каждом complete/_record_failure; coordinator (Remote/Composite) только пробрасывает writer в backend; локальные вызовы в usage.ndjson не пишутся (остаются в local_lifecycle). Resume-безопасность: каждый backend materializes каждый вызов один раз, writer append-only.
2026-08-06. **B9-fix (t_800fedaf): консенсус-выравнивание ужесточено против ложных срабатываний авто-промоута (dry-run run_005).** Офлайн-валидация (t_0251f40c) показала: Master→Блэйк, advantage→получить, anger→чувствовал, blonde→стороны проходили бы в glossary.json. **Причина: (1) proper_name-выравнивание брало доминирующее капитализированное слово совпавших pids без проверки, что это перевод КАНДИДАТА, а не совстречающееся established-имя (Блэйк — значение ключа Blake); (2) term-варианты ранжировались по сырой pid-присутственности, поэтому коллокации, встречающиеся во ВСЕХ pids кандидата и по всей главе (получить/чувствовал/стороны), выигрывали у истинного кандидат-специфичного перевода (преимущество/злость/блондинка). Решение (только эвристики; ledger/контракты B9 и glossary.json не тронуты): proper_name-таргет, равный established glossary-значению ДРУГОГО ключа, отбрасывается (conflict); term-варианты ранжируются по кандидат-специфичности in_count/(1+out_count). Итог на run_005: proposed 6→5 (Master ушёл), ложные пары не промоутятся, Ivy/Rich/Steph корректны.
2026-08-05. **Kanban: цикл «2 карточки» вместо плодения fix-карточек (решение владельца).** B12/B9 показали: каждое замечание ревью создавало fix + re-review карточки (до 25 на задачу) — новые воркеры перечитывают весь незакешированный контекст, растёт расход токенов. **Новый протокол: на задачу РОВНО 2 карточки (I developer + RV reviewer), живут до финала; замечания — в комментариях; цикл дерганий: I blocked(review-required) → RV вердикт: approve → complete обеих; changes requested → ревьюер комментирует замечания + unblock I + RV ждёт → developer переклеймляет I, правит → I blocked → RV ready («проверь меня») → повтор.** Зафиксировано в AGENTS.md (Kanban-практика) и скилле hermes-kanban-orchestration (раздел Review loops — старый паттерн fix/re-review удалён). Пилот — B13.
2026-08-05. **Курсив (em-разметка) в финальном переводе — сохраняем (вариант «а», решение владельца).** Установлено по run_005: первичные кандидаты чистые (0 тегов); `<em>`-теги появляются на этапе formatting (166 в formatted_text) — formatting-модель восстанавливает курсив оригинала (101 em-спан в главе 0001, напр. «*Damn me, damn them, damn it all.*»), но с дефектами: двойное экранирование (`&lt;em&gt;<em>…</em>&lt;/em&gt;`), mixed_script 66 (детектор ругается на токен em). **Решение: курсив сохраняется в финальном переводе** — при записи translations.json из final_translation (B13) HTML-сущности (`&lt;...&gt;`) нормализуются в чистые теги `<em>…</em>`; mixed_script-детектор игнорирует легитимную разметку (теги не считаются мусором); formatting не должен производить двойное экранирование (карточка B14). В модели переводы не передаются — разметка не влияет на промпты.
2026-08-05. **Единый источник финального перевода главы — translations.json (решение владельца).** Подтверждено по коду/артефактам run_005: translations.json содержит только исходные selected-кандидаты (342 pids), committed-ремонты (шаг 7), formatting и карантинные переводы (chunk0005/0016, 58 pids) живут только в repair_report.final_translation (400/400 pids, полный финальный перевод). book_run и B9 читают translations.json — исправления терялись бы при сборке книги. **Решение: финальная запись translations.json полностью перезаписывается из repair_phase_result.final_translation (приведённого к {pid: text}); фолбэк — final_text_by_pid, если repair не выполнялся; формат файла остаётся {pid: text}; инкрементальные записи (resume) не трогаются; quarantined_retry.json для сборки не нужен (всё уже в repair_report: final_translation + метаданные retry).** Переводы в модели не передаются (B9 — 0 модельных вызовов; в промпты идут source+glossary+bible) — тяжёлый repair_report остаётся диагностическим артефактом.
2026-08-05. **run_005_remote (глава 0001, remote, B12-оптимизация) — результаты.** Terminal `accepted_degraded`, 6.84 ч, $1.51, **404 запроса (бюджет 500 НЕ исчерпан)** — B12-батчинг работает (formatting: 15 модельных вызовов вместо ~102). 0 incomplete_generation. Repair: 92 записи (3 раунда), committed 63, debt 29 (12 Gemma re-check failed, 12 qwen re-gate faithful=False, 2 transport, 3 прочее); step7.debt_count 140 = len(debt_trace) — агрегат открытых долгов (repair + reaudit/convergence), НЕ «непереведённые pids»: debt-регион остаётся с исходным кандидатным текстом. narrator_gender «я могла» — **устранена** (в run_003/004 была). **ВАЖНО: translations.json содержит 342 pids (14 selected-чанков), chunk0005 и chunk0016 (58 pids) отсутствуют — их переводы (после успешного quarantined-retry → selected) лежат только в quarantined_retry.json. Сборщик книги (v4_book_run.py) читает только translations.json и НЕ читает quarantined_retry.json → излеченные карантинные чанки выпадут из книги.** Пробел интеграции: мерж retry-переводов в translations.json (или чтение обоих артефактов в book_run) — карточка. mixed_script-список в record — снапшот ДО форматирования; финальный текст чистый (0 из 66 pids с латиницей).
2026-08-05. **B12-F4 (t_f4650418): repair-кэш версионирован с policy — pre-F3 кэш не переиспользуется после fail-closed фикса Qwen вердиктов.** F3 (fail-closed на malformed Qwen-булевых) изменил авторитетную семантику ре-гейта, но identity кэша оставался v1: resume до-F3-рана мог переиспользовать committed=True запись, зафиксированную старым bool("false"). Решение: REPAIR_POLICY_VERSION поднят до pact-v4-repair-policy/v2 (участвует в _repair_unit_hash → все unit-хэши сменились), envelope схемы кэша — до pact-v4-phase4-repair-cache/v2; _load_repair_cache распознаёт legacy v1 как известное поколение и стартует свежий кэш (не foreign-identity, не удаление), новые v2-записи резюмируются детерминированно.

2026-08-05. **B12 (t_9457a3df): батчинг formatting model-fallback и repair re-gate — транспортная оптимизация без изменения гейт-семантики.** Реализация решения владельца «бюджет НЕ поднимаем — сокращаем вызовы/фоллбеки». (1) Formatting: `run_formatting_align` получил `pid_batches` (строгий драйвер передаёт pids каждого чанка); модель-фоллбек, когда каллер умеет `batch`, делает ОДИН вызов на чанк вместо одного на PID (run_004: 78 → 15 вызовов), сохраняя per-PID occupied/проверки и семантику инцидентов (`model_fallback_count` остаётся счётчиком PID, добавлен `model_call_count` — реальные вызовы). (2) Repair: `region_fidelity_gate` получил опциональный `batch`; `_run_repair_round` ре-гейтит регионы одного чанка одним вызовом (95 → 16), вердикт на регион парсится тем же `_parse_qwen_verdict`; каллеры без `batch` сохраняют per-region путь. (3) Convergence re-audit уже скоупирован на changed+neighbours — для главы 0001 изменений нет (в run_004 изменились все чанки). Оценка по run_004 артефактам: полный цикл с formatting 578 → 436 ≤ 500. Гейт-логика, семантика, integrity, identity/resume, политика исчерпания не менялись.
2026-08-05. **run_004_remote (глава 0001, remote, бюджет 500) — результаты и решение.** Terminal `accepted_degraded`, 7.2 ч, $1.79, 500 запросов (бюджет исчерпан ровно). 0 incomplete_generation (B10); repair committed 73 (vs 32 в run_003); quarantined_retry ran (vs failed в run_003) — chunk0005 `quarantined_final` (оба новых кандидата снова не прошли Qwen-гейт: «я думал/я думала», «убил», «Я спрашивала»). НО formatting (шаг 8) снова не доработан: 102 инцидента, 78 model-fallback (модель не вызывалась — бюджет кончился) → mixed_script 75 pids (в основном HTML-теги em, заэкранированные моделью в сущности) + narrator_gender «я могла» не исправлена (та же, что в run_003; не покрыта repair-регионами — детерминированный гейт не создаёт findings). Вывод: полный цикл главы ~600–650 запросов. **Решение владельца: бюджет НЕ поднимаем — оптимизируем: сократить количество модельных вызовов и фоллбеков** (formatting 78 fallback, repair-вызовы 98) — карточка B12. D1 (usage-record) остаётся; пол персонажей (Пэйдж/Айви) в промпт генерации — отдельная задача на улучшение промптов.
2026-08-05. **Сравнения больше не проводим (решение владельца).** strict single-resident driver остаётся производственной архитектурой v4 (подтверждение решения 2026-08-01); batch-first/sequential — экспериментальные/measurement-инструменты, дальнейшего non-inferiority сравнения batch-first vs strict **не будет**. **Phase 0D (pre-registered non-inferiority policy) ОТМЕНЕНА** как обязательный гейт: формальный v3/v4 benchmark (golden set, blind rubric, one-sided margins) проводиться не будет. Принятие качества v4 — на основе валидационных прогонов (run_002/003/004 главы 0001, study run_006 vs run_003_remote на главе 046) и решений владельца. **D3 (Phase 7 A/B v3/v4)** упрощается до решения владельца по итогам валидации — без pre-registered gate. **D1 (Phase 6 usage-record)** — остаётся в плане.
2026-08-05. **run_003_remote (глава 0001, remote opencode 1.4.7, B9/B10-код) — результаты.** Terminal `accepted_degraded` (лучше run_002: failed). 16/16 чанков, selected 15, quarantined 1, **incomplete_generation 0** (vs 3 в run_002 — B10-ретраи работают: 8 транзиентных EmptyResponse/TruncatedJSON все отретраены), wall 15128 c (~4.2 ч), cost $1.11 (250 запросов: 295k in / 487k out / 608k cached). step6 audit complete (108 findings/95 regions). НО `remote_budget max_requests_per_chapter=250` **исчерпан** в конце: convergence re-audit, repair (183 debt), formatting (102 инцидента, 78 model-fallback — модель не вызывалась), quarantined retry — **failed**. narrator_gender integrity поймал реальный дефект «я могла» (женский род при мужском POV) — не исправлен из-за budget-debt. Вывод: 250 мало для полного цикла главы; полный цикл ~350–400, запас — **500**; `remote_budget` в runtime-конфиг не проброшен (дефолт зашит в код) → карточка B11 (параметризация + дефолт 500). Политика исчерпания (GateResult, не прерывать) — остаётся (решение владельца 2026-08-04).
2026-08-04. **B9-F9 (t_bd503c99, review B9-RV6 MEDIUM): docs-only — scope B9 зафиксирован glossary-only, семантика `committed` приведена к фактическому коду.** (1) Карточка B9 §2 больше не оставляет открытый выбор «glossary-only или включить book_memory»: B9 добавляет ТОЛЬКО категорию `glossary` через `MemoryManager.add_observation` -> `promote` (B7), `book_memory.json`/`book_memory_hash` не меняются (V-финал). (2) Docstring/комментарии `v4_book_run.py` и модульный docstring B9-интеграционных тестов приведены к фактическому коду: после F5/F6+RV3 quarantined pids исключаются ДО генерации, поэтому B9-сгенерированные observations несут только accepted `chunk_id` и `committed == proposed` для complete И accepted_degraded (валидный план); B7-фильтр остаётся defense-in-depth и может дать `committed < proposed` только для независимых ручных observations с quarantined `chunk_id`. Формулировка записи B9-F2 ниже («B7 quarantined-фильтр может дать committed < proposed для accepted_degraded») в этой части УСТАРЕЛА для B9-сгенерированных observations. Production-логика не менялась; целевые B9-тесты зелёные.

2026-08-04. **B9 (F6, review B9-RV5): quarantined accepted_degraded — fail closed при неоднозначном (duplicate) PID→chunk ownership.** Если `chunk_plan.json` мапит один PID более чем на один chunk (или дважды внутри одного чанка), дублирует/теряет chunk identity или содержит malformed/non-list chunk/PID данные, B9-цикл больше НЕ перезаписывает ownership последним чанком: duplicate/malformed план неавторитетен (контракт `ChunkPlanArtifact` отвергает duplicate ownership) → fail closed — ноль кандидатов, ноль строк в ledger `glossary_candidates.json`, ноль наблюдений, ноль мутаций glossary (warning в лог, book run не падает). Ранее последний (accepted) чанк молча перезаписывал quarantined-владение того же PID, и quarantined-свидетельства давали кандидата/авто-промоут (B9-RV5, HIGH, data integrity). Полные главы и валидный уникальный mapped accepted_degraded (mixed-chunk подсчёт только по accepted PID/чанкам) не меняются.

2026-08-04. **B9-F3 (t_e3f9b387): кумулятивный target-конфликт ledger'а блокирует авто-промоут (фикс ревью-находки HIGH B9-RV2).** Промоут кандидата требует, чтобы кумулятивная запись ledger сохраняла ровно один недвусмысленный target, совпадающий с текущим выровненным target главы; запись с target None из-за разных target по главам (targets_seen > 1, слияние необратимо) считается conflict и никогда не промоутится — иначе неоднозначное source->target попадало бы в glossary.json. Порядок (после _run_one_chapter, до promote), пороги v3, quarantine-фильтр B7, плоский {source: target} и 0 модельных вызовов не меняются.

2026-08-04. **B9 (F5, review B9-RV4): quarantined accepted_degraded — fail closed при недоступном PID→chunk provenance.** Если `selection_results.json` объявляет quarantined-чанки на `accepted_degraded`-главе, B9-цикл допускает генерацию кандидатов ТОЛЬКО когда `chunk_plan.json` авторитетно исключает все quarantined-свидетельства: missing/corrupt/empty/incomplete план (source/translation PID без маппинга) = fail closed — ноль кандидатов, ноль строк в ledger `glossary_candidates.json`, ноль наблюдений, ноль мутаций glossary (warning в лог, book run не падает). Ранее пустой/битый план трактовался как «нет чанков» → непроверенные PID попадали в ledger и авто-промоут (B9-RV4, HIGH, data integrity). Полные главы и валидный mapped accepted_degraded (mixed-chunk подсчёт только по accepted PID/чанкам) не меняются.

2026-08-04. **B9-I3: политика наблюдений глоссария — V-финал (решение владельца 2026-08-04).** Без модельных вызовов: кандидаты — частотные имена/термины из source-текста главы, отсутствующие в `glossary.json`/`book_memory.json`; target извлекается консенсус-выравниванием по готовому переводу (pid→pid, доля варианта >= 0.8); авто-промоут по v3-порогам (proper >= 2, term >= 2 глав и >= 3 вхождений, 1 вариант); несогласованность → ledger `conflicts` (не промоутится); ledger `glossary_candidates.json` в out-base; промоут через `observations` + `promote` (B7). Старый Вариант B (модельный target) отменён. Обязательный шаг перед первым боевым book-run — офлайн-валидация авто-промоута на артефактах главы 0001 (`run_002_remote`): прогнать генератор + промоут, выложить, что промоутнулось (proposed/committed/conflicts).

2026-08-04. **B9-F2 (t_63a24eb1): семантика per-chapter блока `candidates` в book_run.json — `proposed`/`committed`/`conflicts` (владелец: авто-промоут со строгими свидетельствами — V-финал, НЕ shadow-only; исторический ярлык «Вариант B» отменён — канон в записи B9-I3).** Запись B9-I2 follow-up (Variant A shadow-only) устарела и отменена: владелец подтвердил авто-промоут (V-финал; запись того времени использовала ярлык «Вариант B» — ярлык отменён как неоднозначный, канон — запись B9-I3; `docs/plans/V4_B9_GLOSSARY_OBSERVATIONS_TASK_RU.md`; пороги v3 не меняются). B9-цикл снова вызывает `MemoryManager.add_observation` -> `promote` (B7), сохраняя принятые F2-защиты: гейтинг на terminal_status (F1), B5 combined allowlist в генераторе (F3), консервативный term-target (co-occurrence guard). Семантика полей блока `candidates`: `generated` = выровненных кандидатов главы; `proposed` = отправлено в `add_observation` в этой главе (v3-пороги + единственный target + нет established-конфликта); `committed` = сколько из proposed реально попало в glossary.json после promote (diff glossary до/после; B7 quarantined-фильтр может дать committed < proposed для accepted_degraded; complete => committed == proposed); `conflicts` = не промоутится (alignment-конфликт или established-конфликт с другим target). 0 модельных вызовов; `MemoryManager` не менялся; glossary/observations вне снапшота и identity — cache/resume не инвалидируется. REVIEW REQUIRED — независимый review (B9-RV4).

2026-08-04. **B9 (F4, review B9-RV3): политика наблюдений — авто-промоут (V-финал; исторический ярлык «Вариант B» в этой записи отменён — канон в записи B9-I3), утверждён владельцем.** Владелец явно утвердил авто-промоут — V-финал (комментарий архитектора 2026-08-04, t_772820ec/t_ecabd4bb; в записи того времени — «Вариант B»): авто-промоут кандидатов глоссария в glossary при complete остаётся, но ТОЛЬКО со строгим консервативным source→target-доказательством. Запись F2 (725c20b) «Вариант A shadow-only, явного утверждения B нет» — УСТАРЕЛА и отменяется настоящей. Реализация: B9-цикл в `v4_book_run` после главы ДО `MemoryManager.promote` (GATE) генерирует кандидатов + консенсус-выравнивание (pid→pid, consensus 0.8, contrast/exclusions), пишет их в append-only ledger `glossary_candidates.json`, затем `_auto_promote_glossary` вызывает `MemoryManager.add_observation` для кандидатов, прошедших v3-пороги (proper_name ≥2 с одним target; term ≥2 глав И ≥3 вхождений с одним target), и штатный B7 `promote` переносит наблюдения в glossary при complete/accepted_degraded; flat-контракт `{source: target}` восстанавливается после promote. Строгая консервативность: термин-выравнивание отбрасывает target, если два term-кандидата главы доминируют на одном русском варианте (ко-оккурентность ≠ перевод) — bound/together рядом с pact никогда не получают «пакт» (регрессия test_alignment_co_occurring_terms_cannot_share_unrelated_target + интеграция). Гейты доказательности (B9-RV3): (1) в ledger и в авто-промоут попадают только принятые терминальные статусы (complete/accepted_degraded) — failed/unknown/error главы не учитываются и не могут закрыть пороги; (2) quarantined-чанки исключаются ДО аккумуляции ledger и авто-промоута на уровне pids (их текст не порождает кандидатов и не формирует target; кандидат целиком из quarantined-чанка не имеет записи в ledger и не промоутится, смешанный кандидат учитывает только accepted-chunk occurrences); (3) B5 manual allowlist берётся из того же `--mixed-script-allow`/`deterministic_mixed_script_allow`, что и strict-драйвер (нет отдельного `--candidate-mixed-script-allow`), combined = bible + glossary + manual + source-derived. Lifecycle/cache: ledger вне снапшота и config identity (проверено B9-I1) — B9 сам по себе не инвалидирует cache; авто-промоут B9 добавляет только category `glossary` → меняется только `glossary.json` (flat `{source: target}`), `book_memory.json` и `book_memory_hash` НЕ меняются (уточнено B9-RV3, сверка с `_book_memory_hash()` в `v4_book_run.py`), следующая глава читает обновлённый glossary; resume безопасен (promote идемпотентен, ledger append crash-safe, конфликты established/locked не перезаписываются). REVIEW REQUIRED — утверждённый владельцем V-финал (исторический ярлык «Вариант B» отменён), реализация F4.

2026-08-04. **run_002_remote — пост-прогонная проверка generation_outcomes.json (закрытие блокера из записи ниже).** Прогон главы 0001 завершён: terminal `failed` (86 formatting-инцидентов, blocking). `generation_outcomes.json`: 16 чанков, 3 `incomplete` (chunk0010/0012/0015) — но в каждом из них ОБА кандидата присутствуют (`present: true`, error-поля пустые): систематического пустого/обрезанного JSON в генерации НЕТ, причина incomplete — не пустой ответ, а статус чанка/валидация кандидата (error-поля пустые, нужна диагностика по journal/selection). Вывод: блокер «проверить generation_outcomes.json после прогона» закрыт; решение по retry/диагностике покрывает B10 (B4 JSON-resilience на все роль-адаптеры + remote-бюджет 250 + диагностика пустых ответов); точный код причины incomplete — уточнить в B10 по артефактам.

2026-08-04. **B9-I2 follow-up (review PR #128, B9-RV2): политика наблюдений — Вариант A (shadow-only).** Явного утверждения владельцем Варианта B (авто-промоут кандидатов в glossary при complete) нет — карточка B9 задаёт «по умолчанию Вариант A, окончательно — решение владельца», поэтому production-интеграция переведена на shadow-only: генератор + консенсус-выравнивание пишут кандидатов только в shadow-ledger `glossary_candidates.json`; из book-run `MemoryManager.add_observation` не вызывается, `glossary.json`/`observations.json` B9-цикл не мутирует, рост глоссария остаётся ручным (человек ревьюит ledger и правит glossary/наблюдения сам). Заодно: (F1) в ledger попадают только принятые терминальные статусы (complete/accepted_degraded) — failed/unknown/error главы не учитываются и не могут закрыть пороги; (F2) term-выравнивание консервативно: если два term-кандидата главы доминируют на одном русском варианте, оба лишаются target (ко-оккурентность ≠ перевод) — несвязанные совстречающиеся source-термины не промоутятся как один несвязанный target; (F3) в генератор передаётся B5-комбинированный allowlist (bible+glossary+manual+source-derived), allowlist-токены кандидатами не становятся. Lifecycle/cache: ledger вне снапшота и config identity (проверено B9-I1) — B9 сам по себе не инвалидирует cache; `book_memory_hash`/identity B9 не меняет; `promote` (B7) по-прежнему переносит только существующие (ручные) наблюдения. REVIEW REQUIRED — решение владельца подтвердить/изменить. **ОТМЕНЕНА** — заменена записью B9-I3 (V-финал: авто-промоут со строгими свидетельствами); сохранена как исторический след отменённой shadow-only политики.

2026-08-04. **run_002_remote (глава 0001, remote-бекенд opencode serve): 3× `incomplete_generation` — обязательная проверка перед движением дальше.** В remote-прогоне `v4_phase12_strict_0001/run_002_remote` три чанка (chunk0010, chunk0012, chunk0015) ушли в `incomplete_generation`: одна из двух ролей не прошла валидацию (fidelity_first в 0010/0012, balanced_literary в 0015; 2 backend-события = оба вызова сделаны, один ответ отклонён валидатором). Локальный run_001 того же chapter — 0 incomplete_generation (12 selected, 4 quarantined). Гипотеза владельца «gemma передаёт один перевод, если оба почти одинаковые» кодом не подтверждается: Phase 2B — два отдельных вызова (fidelity_first/balanced_literary, `prompts.py`), правила «один вместо двух» нет; `incomplete_generation` = ответ роли не прошёл валидацию (invalid JSON / PID mismatch / context leakage). ПЕРЕД продолжением B-серии: после завершения run_002_remote проверить `generation_outcomes.json` (пишется в конце прогона) → точный GenerationErrorCode упавших ролей → отделить обрыв remote-канала от систематического невалидного JSON → решить, нужен ли retry упавшей роли (если да — отдельная карточка).

2026-08-04. **Решения после B8 (валидация B4–B7, владелец).** (1) 6 provisional-записей v3-глоссария (incarnation/echo/Junior Council/Chronomancer/Ogre Magi/Binding) — **НЕ включаются** в production glossary.json (119 записей остаются): incarnation/echo помечены «human translation is inconsistent», Chronomancer — единичное вхождение, Binding — context-dependent guidance (0 вхождений); пересмотр — при прогоне соответствующих глав. (2) Сбор наблюдений глоссария в проде — отдельная задача **B9** (карточка `docs/plans/V4_B9_GLOSSARY_OBSERVATIONS_TASK_RU.md`): перенос v3-механики генератора кандидатов (`pact_translate_v3.py` glossary_candidates) в v4 + вызов `add_observation` (`MemoryManager`, `pact_v4/phase1/memory.py`) в `v4_book_run` после главы до `promote` (B7); shadow → `observations.json` → promote при complete/accepted_degraded. B9 параллельна Потоку D и НЕ блокирует B8-прогон/D1: `observations.json` не входит в снапшот/identity и не влияет на промпты/гейты; кандидаты по главе 0001 собираются офлайн из артефактов B8-прогона (генератор детерминированный, модели не нужны). B8-прогон главы 0001 выполняется на чистом main. Политика наблюдений (shadow-only vs авто-промоут) — на реализацию B9.

2026-08-04. **Созданы production-входы `book_memory.json` + `glossary.json` для книги Bonds (главы 46/60/100/112/148) — объединение всех найденных старых библий v3 + перенос v3-глоссария.** Контекст: в v4 не существовало ни библии, ни глоссария (запись B8 ниже говорила об этом как о фиксированном факте — устарело; теперь входы созданы, B7-библия/narrator_gender в B8 получат реальные данные). Причина: B7 рендерит `book_memory.json` в промпты и narrator_gender check активируется только при наличии `pov.gender`, а v3-глоссарий (90 established + 29 locked + 6 provisional записей в `glossary/established.json`/`locked.json`/`provisional.json`) в v4 не переносился — v4 читает единый `glossary.json`. Что сделано: (1) найденные уникальные библии (11 md5-уникальных файлов по всему диску: v3 pipeline_runs/benchmark_results ch60/ch100, gate_bench_runs ch46 ×4, book_bible_test112/148, вручную исправленная `benchmark_inputs/chapter148_corrected/chapter_bible.json` от 2026-07-24) объединены в `D:/pact/pact_chapters/book_memory.json` (63 персонажа, 27 сущностей, 28 фактов, 9 address-записей, `pov.gender=male`, индекс глав 46/60/100/112/148) — это директория `--memory-dir` production strict-драйвера, туда пайплайн уже смотрит; (2) v3-глоссарий (established 90 + locked 29; provisional 6 — НЕ включены, их notes требуют проверки перед промоушеном) слит в `D:/pact/pact_chapters/glossary.json` (119 записей, формат `{source: target}` как ожидает `_glossary_entries`); (3) скрипты сборки — `D:/pact/pact_translator_worktrees/_bible_merge_build/build_book_memory.py` + `build_glossary.py`. Решения владельца: narrator_gender — обязательно male (Blake Thorburn, first person); Blake/Blake Thorburn — один персонаж, канонический ключ «Blake Thorburn», target «Блэйк Торбёрн», формы имени (Blake/Блэйк) — в `variants` (библия), переводные пары — в glossary; Mags (Мэгс, гл. 112/148, медиатор совета) и Maggie (Мэгги, гл. 60, спутница с Hyena) — РАЗНЫЕ персонажи (подтверждено вручную исправленной библией 148 «Mags fixed instead of Maggie»); register Blake→Duncan — «вы» (v3-версия, не v31 «ты»); факт про гулей — «eggs» (v3, не v31 «holly»); The fat ghoul = The fat man — один персонаж; гл. 46 — union всех 4 gate-прогонов; гл. 148 — авторитет вручную исправленная библия. Проверено: `ChapterMemory.from_directory` грузит оба файла, `_glossary_entries` парсит 119 записей, `render_bible_section` даёт корректный `BIBLE:` блок, комбинированный mixed-script allowlist 196 токенов, 41 тест B7 зелёные. Глоссарий в v4 пополняется вручную (генератор кандидатов v3 `glossary_candidates.*.json` в v4 не перенесён — `add_observation` в проде не вызывается, только в тестах; `promote` в `v4_book_run` работает). PR: rides on #127 (B8) как docs-only.

2026-08-04. **B8 (повторный прогон главы 0001) — блокирующий фикс CLI strict-драйвера + --run-label.** При подготовке B8 (валидационный прогон, docs/plans/V4_B8_CHAPTER_0001_REVALIDATION_TASK_RU.md) выяснилось: реальный запуск v4_phase12_strict_run.py на текущем main падает на старте — регрессия из PR B5 (17f191f): _build_run_config читает несуществующий атрибут Namespace max_consecutive_terminal_nonselections вместо max_consecutive_nonselections (до B5 код был верным; B6/B7 прогоны CLI не исполняли, поэтому не поймали). Исправлено (восстановлено args.max_consecutive_nonselections). Также добавлен --run-label (по умолчанию — исторический v4-phase12-strict-chapter-trial): run_label не входит в config_identity (см. to_config_artifact), но пишется в record, поэтому повторный прогон получает собственный label (v4-phase12-strict-0001-run002) без изменения identity-цепочки. На момент написания записи production-входов book_memory.json/glossary.json для Bonds не существовало (v3 book_bible.json пустой: characters: {}) — с 2026-08-04 они созданы (см. запись выше), B7-библия/narrator_gender в B8 получат реальные данные. PR: draft.

2026-08-04. **B7 deployment** — merged PR #126 (merge commit `f6a3f1d`, body commit `5320f31`) to `main`; production tree at `D:\pact\pact_translator_v4` fast-forwarded from `2639c00` to `f6a3f1d` (pre-deploy backup tag `archive/v4-pre-b7-20260804-083934`). Post-deploy: 141 unit/integration tests green (B7 + generation + backend_role_adapters + quarantined_retry + strict_runner_retry); bible_renderer / narrator_gender / MemoryManager.promote / v4_book_run / build_role_adapters / build_repair_adapters imports confirmed; sample bible render produces correct `BIBLE:` block. No active pipeline (llama-server process not running). Pipeline restart requires explicit `Запускай` command.

2026-08-04. **B7 (библия + междуглавная + book-run, draft PR)** — рендер `book_memory.json` в промпты генерации/fidelity/аудита, narrator_gender check, promotion при `accepted_degraded`, book-run wrapper. Причина: run_001 выявил смену рода рассказчика (chunk0015 «увидел» → chunk0016 «увидела»), ни один слой не поймал — библия загружалась в `ChapterMemory`, но никогда не рендерилась в промпты (`_glossary_entries` извлекал только glossary). Решение: (1) новый модуль `pact_v4/runtime/bible_renderer.py` — `render_bible_section(book_memory)` рендерит narrator/characters/facts/address_register в `BIBLE:` блок, `extract_narrator_gender(book_memory)` извлекает пол из `pov.gender` или legacy-ключа; (2) `bible_text` добавлен в `PromptBundle` (generation identity, часть `bundle_hash`), `render_prompt` (generation), `render_qwen_review_prompt` (fidelity), `render_qwen_audit_prompt`/`render_gemma_audit_prompt` (audit) — bible_text передаётся через **все** adapter configs (`BackendQwenEvaluatorConfig`/`BackendQwenAuditEvaluatorConfig`/`BackendGemmaAuditEvaluatorConfig`/`HttpQwenEvaluatorConfig`); `v4_phase12_strict_run.py` грузит `book_memory.json` и рендерит библию ДО `build_strict_lifecycle`/`build_role_adapters`/`build_repair_adapters` (path A, выбран из двух вариантов review: minimal config-time change вместо runtime setter); `build_role_adapters` и `build_repair_adapters` в `runtime_config.py` принимают `bible_text`; lifecycle-адаптеры и `build_strict_lifecycle` тоже; B6 retry cycle (`pact_v4/phase4/quarantined_retry.py::run_quarantined_retry`/`_retry_one_chunk`) принимает и пробрасывает `bible_text` в `generate_for_chunk` — retried чанки теперь генерируются с той же библией, что и main loop (был рассинхрон, B6 promotion мог дать другие кеш-ключи); (3) `check_narrator_gender(text, expected_gender)` в `_integrity_checks.py` — regex-скан «я + past-tense verb» на согласование рода, вызывается в strict runner после Step 7/8 на финальном тексте, mismatch → `integrity.narrator_gender` + `step8.status = accepted_degraded` (если был `complete`); (4) `MemoryManager.promote(status, quarantined_chunks=...)` — `accepted_degraded` promote'ит наблюдения, исключая quarantined chunks (по `chunk_id` в observation), `complete` promote'ит все, `failed`/`quarantined` не promote'ит; (5) book-run wrapper `pact_full_pipeline_runner_v1/v4_book_run.py` — CLI `--memory-dir --chapters --chapter-html-pattern --out-base`, последовательный вызов strict runner + promote после каждой главы, артефакт `book_run.json` (per-chapter terminal status, book_memory_hash before/after, promotion events), exit code 1 если хоть одна глава не достигла `complete`/`accepted_degraded`. Identity: `bible_text` — часть `bundle_hash` (generation) и промптов (fidelity/audit), смена библии инвалидирует cache; `book_memory_hash` в снапшоте уже покрывает resume. Scope: phase1/2, cascade, risk не тронуты (кроме передачи библии в промпты); v3 не тронут. REVIEW REQUIRED: `bible_text` добавлен в промпты — изменение того, что видит модель; бюджет ограничен (top-20 персонажей, top-30 фактов, top-10 address entries) + suffix `(showing first N of M)` при truncation; реальный impact на качество нуждается в валидации в B8.

2026-08-03. **B6 (quarantined-чанки: отдельный цикл ремонта, draft PR)** — принята политика по quarantined чанкам с repair-долгом (`docs/plans/V4_B6_QUARANTINED_RETRY_TASK_RU.md`, Вариант A с Вариантом B как fallback). Причина: run_001 — quarantined чанки (chunk0005/0009/0010 — qwen_fidelity; chunk0001 — mixed_script, разблокирован B5) оставались «недовыпущенными»: их best-variant попадает в `repair_report.final_translation`, но repair-долг остаётся и `complete` недостижим, т.к. при падении обоих кандидатов на одинаковых findings repair не может закрыть finding без новой генерации (модель не видела диз-амбигвирующий контекст: chunk0005 p00095/p00099, chunk0009 `grandchild`→`внук`, chunk0010 `well after dark`). Решение: после B2 repair, если quarantined чанк имеет repair-долг, запускается отдельный bounded (1 retry) цикл — перегенерация чанка с look-ahead right_context (source следующего чанка) + повторный cascade (phase1/2, cascade, risk, prompts не тронуты, кроме передачи look-ahead context). Выигравший кандидат заменяет best-variant, чанк re-audit'ится (`_reaudit_chunks`) и ремонтируется одним раундом (`_run_repair_round`); чанк, всё ещё не прошедший cascade, признаётся финальным с best-variant (`quarantined_final: true`, Вариант B). Новый модуль `pact_v4/phase4/quarantined_retry.py` (backend-neutral, dual-mode import guard); драйвер (`v4_phase12_strict_runner.py`) вызывает цикл после Step 7/8 и перерешает терминал. Артефакты: `quarantined_retry.json` (история попыток chunk_id/attempt/outcome, foreign-identity-валидируется на resume) + `repair_report.json` получает `quarantined_final`/`retry_attempts` (+ блок `quarantined_retry`); новые кандидаты мержатся в `generation_outcomes.json`. Identity: source/snapshot/chunk_plan не меняются; resume переиспользует прошлую попытку без повторной генерации (bounded 1 retry).

2026-08-03. V4 B5 (mixed_script-политика, draft PR): allowlist легитимных латинских токенов вместо транслитерации. Комбинированный allowlist = библия (источник — **v4 `book_memory.json`**, персонажи/факты/address register/voice notes по V4_MVP_SPEC_RU.md §6; `book_bible.json` в названии карточки — ошибка, реальная v4 `book_bible` ещё не реализована — это B7) + глоссарий (glossary.json) + source-derived + manual `deterministic_mixed_script_allow` (StrictRunConfig/CLI `--mixed-script-allow`); `final_allow` передаётся в find_mixed_script в cascade (Phase 2C), audit (Phase 3B) и repair (Phase 4) через DeterministicGateData.mixed_script_allow — cascade/risk/prompts не изменены. Ключевое решение: allowlist-записи токенизируются тем же `_SCRIPT_TOKEN_RE`, что и find_mixed_script, поэтому запись «R.D.T.» разблокирует токены R/D/T (иначе точное сравнение целой строки «r.d.t.» никогда не совпало бы с токеном). Source-derived — по правилу «токен есть в source И в translation»: per-chunk (кандидаты чанка) для cascade, whole-chapter (все кандидаты + committed) для Step 6/7; union кандидатов даёт ровно эту семантику для каждого проверяемого текста, не ослабляя гейт для токенов, отсутствующих в source. Identity: библия = `book_memory`, уже в снапшоте через существующий `book_memory_hash` (смена библии/глоссария/source инвалидирует cache/resume без нового поля); manual allowlist добавлен в config_identity (по прецеденту sequential_runner). Scope (review PR #124, п.1): auto-population только в strict-раннере (production driver); `draft_runner`/`sequential_runner` остаются manual-allowlist-only до явного решения — при будущем переходе на sequential нужно перенести ту же политику. Осознанный трейд-офф (REVIEW REQUIRED): glossary-source-термины и whole-chapter source-derived ослабляют mixed_script-детекцию англоязычного остатка, т.к. любой EN-токен, присутствующий в source, становится легитимным; backstop — glossary_consistency-гейт (остаётся blocking). Разблокирует chunk0001 p00013 «R.D.T.» (единственный структурный блокер run_001). Решения владельца 2026-08-03 (после ревью PR #124): (1) отдельный `names`-layer не вводим — source-derived остаётся покрытием имён, присутствующих в source; имена вне source/glossary ловит mixed_script; (2) `draft_runner`/`sequential_runner` остаются manual-allowlist-only (strict — production driver), перенос политики — при явном переходе на sequential; (3) source-derived для Step 6/7 остаётся whole-chapter, per-chunk/per-PID-ограничение и per-chunk-трейс в record не вводим.

2026-08-03. B4 (JSON resilience, draft PR): bounded retry для пустого/обрезанного JSON в qwen-audit (Step 6) и repair (Step 7). Явные классы ошибок EmptyResponseError/TruncatedJSONError (pact_v4/runtime/json_resilience.py): retry'ится только пустой или не-parseable JSON; transport failure (CompletionError/C1 OpenCodeError) — отдельный класс, никогда не retry'ится как JSON error (transport retry остаётся доменом C1). Retry реализован в ролевых адаптерах BackendQwenAuditEvaluator/BackendRepairCaller: re-issue идентичного CompletionRequest (тот же prompt/backend/request_options) → identity/resume не меняется, 
easoning=0 из B1 соблюдается (request_options не добавляются). max_retries=2 по умолчанию (3 попытки), exponential backoff, override через json_retry_policy в uild_role_adapters/uild_repair_adapters (runtime config, опционально; политика resilience — не часть backend identity). Retry исчерпан → failed unit (audit) / debt (repair), никогда semantic verdict (правило «never accept truncated JSON» сохранено).

2026-08-03. Обнаружена смена рода рассказчика в конце главы 0001 (chunk0015 p00366 «увидела», chunk0016 целиком женский; p00381 «увидел» → p00386 «увидела») — ни один слой пайплайна не поймал. Добавлен раздел 7 в [`docs/audits/V4_PHASE12_STRICT_0001_RUN001_ANALYSIS_RU.md`](docs/audits/V4_PHASE12_STRICT_0001_RUN001_ANALYSIS_RU.md). Корень: (а) fidelity-гейт проверяет верность источнику текущего чанка (source gender-neutral), межчанковая граница вне scope; (б) book_memory никогда не подаётся в модель — `_glossary_entries` извлекает только glossary, проза библии не рендерится; (в) нет инварианта согласования рода. Рекомендации: подавать канонический пол рассказчика в fidelity-гейт/аудит, добавить категорию narrator_gender в qwen_chapter_audit или детерминированный regex-скан по всей главе.

2026-08-03. Исчерпывающий разбор live-прогона `gate_bench_runs/v4_phase12_strict_0001/run_001` (глава 0001_bonds-1-1, strict chapter trial, 400 pid / 16 чанков, wall clock ~18,6 ч, код до PR #116/#117) сохранён в [`docs/audits/V4_PHASE12_STRICT_0001_RUN001_ANALYSIS_RU.md`](docs/audits/V4_PHASE12_STRICT_0001_RUN001_ANALYSIS_RU.md). Выводы и решения: (1) прогон дошёл до терминала `accepted_degraded` без ранней остановки и потери данных; identity-цепочка (source/snapshot/chunk-plan/config/backend) консистентна во всех артефактах. (2) Генерация полная (32/32), отбор 12 selected / 4 quarantined (chunk0001 — mixed_script p00013, chunk0005/0009/0010 — qwen_fidelity: пропуск/род p00095, `grandchild`→`внук` p00193, «well after dark»→«далеко за полночь» p00239). (3) step6 `incomplete` из-за пустого ответа qwen_chapter_audit для chunk0011 (`Reject partial or invalid JSON: Expecting value: line 1 column 1`); 114 findings / 95 регионов (calque 30, scene 25, addition 12, dialogue 11, omission 10, referent 8, register 6, ty_vy 6, repetition 5, mixed_script 1). (4) step7: 187 правок / 93 committed / 82 долга; **единственный структурный блокер — mixed_script «R.D.T.» в p00013** (легитимные латинские инициалы в источнике; 8 правок chunk0001 и финальный `integrity: failed` упираются в него); остальной долг — провалы re-gate и ≥1 обрезанный JSON при repair. (5) formatting: 101/102 спана, 1 блокирующий инцидент (p00207 `target_not_found`), 78 через model fallback. (6) `translations.json` = 297/400 pid (74,25%), только committed чанки, без ремонта/форматирования; полный 400-pid текст — только в `repair_report.final_translation`. Решение по политике: `accepted_degraded` остаётся корректным терминальным статусом доступности (валидный PID-map + debt trace, без memory promotion) и **не** является `complete`. Действия на будущее: (а) решить mixed_script-политику для легитимных латинских инициалов/имён (allowlist или транслитерация) — разблокирует chunk0001 и integrity; (б) добавить retry/устойчивость к пустому и обрезанному JSON на этапах qwen-audit и repair (incident risk, «never accept truncated JSON»); (в) отдельный цикл ремонта quarantined-чанков, т.к. re-gate для chunk0005/0009/0010 систематически не проходят; (г) прогон был на коде до PR #116/#117 — L1/L2b/L3 (батчинг регейтов, узкий регион-регейт, severity-фильтр soft-findings) и phase-progress-трекер в этот прогон не входили, их эффект оценивается отдельно на следующем прогоне.

2026-08-03. Решение по отслеживанию прогресса прогона по фазам (карточка `docs/plans/V4_PHASE12_RUN_PROGRESS_TRACKER_TASK_RU.md`). Причина: в середине Step 6 (аудит) и Step 7 (ремонт) нет ни одного артефакта с прогрессом — `audit_cache/b2_handoff/audit_findings` пишутся атомарно в конце Step 6, `repair_cache/repair_report` — только в конце Step 7; единственный «монитор» — `server_logs/`, из которого не видно ни текущего юнита/чанка, ни числа отремонтированных регионов. Принято: (1) **новый write-only артефакт `phase_progress.ndjson`** (append-only, crash-safe, события started+done по точкам: `run_started`, `chunk_started/done`, `audit_unit_started/done` по `(chunk_id, detector)`, `repair_round_started`, `region_started/done` (repair_id, target_pids, action, committed/debt, reason), `reaudit_unit_started/done`, `formatting_done`, `terminal`); артефакт **не влияет** на логику пайплайна, resume, кэш-identity, journal schema, терминальные статусы — диагностика, не статус; (2) **read-only трекер CLI** `python -m pact_full_pipeline_runner_v1.v4_phase_progress --out-dir <run_dir> [--watch <сек>]` с выводом: фаза с основанием вывода, per-chunk таблица «сколько передано и куда» (триал → аудит → ремонт → форматирование/терминал), счётчики по фазам, текущая модельная активность; (3) **graceful degradation** — без `phase_progress.ndjson` (старые прогоны, текущий `run_001` на старом коде) переход на грубую инференцию по наличию артефактов. Реализация строится на уже влитом в `main`, но не задеплоенном коде (PR #116, `f867194`): Step-7-вью отражает роль-проходы L2b и читает `repair_report` в актуальной схеме (`rounds`/`debt_trace`/`formatting`). Ограничения: трекер read-only (не пишет в out_dir, не трогает пайплайн), identity/кэш не меняются, деплой до остановки текущего прогона не выполняется.

2026-08-03. Решение по оптимизации вызовов моделей в Phase 4 repair (Step 7) после разбора live-прогона главы 0001 (`gate_bench_runs/v4_phase12_strict_0001/run_001`): раунд 1 — ~310 модельных вызовов и ~190 переключений на 95 регионов; доминирует Qwen re-gate по всему чанку (~3 мин / ~5200 токенов на регион), который затем дублируется convergence re-audit тех же чанков теми же моделями. Принято (карточка `docs/plans/V4_PHASE4_REPAIR_CALL_OPTIMIZATION_TASK_RU.md`): (1) **L1** — батчинг `_reaudit_chunks` по детектору (`for detector: for chunk:`, то же обоснование, что в записи 2026-08-01 для Phase 3), нулевое семантическое изменение; (2) **L2b** — узкий регейт по правленому региону (новый промпт-вариант `region_fidelity_gate`) и батчинг раундов 1/2 в проходы по ролям (все правки Gemma → все регейты Qwen → все речеки Gemma → батчированный re-audit), коммит откладывается до конца прохода, критерии коммита (fidelity passed + gemma-recheck passed где требуется) и `repair_id`/кэш-идентичность не меняются; (3) **L3** — severity-фильтр мягких Gemma-находок (`calque`/`register`, опц. `repetition`) только по силе доказательства (короткий excerpt / неуверенная note), soft-категории не являются blocking в раунде 2, пропущенные фиксируются в debt trace. Инвариант сохранён: семантический верификатор ≠ модель-редактор (регейт Qwen, редактор Gemma, урок v3); поэтому предел — две модели, роли уже максимально разделены (Gemma = редактор + русский речек, Qwen = регейт + re-audit). Осознанные трейд-оффы: правки внутри чанка готовятся по одному срезу текста (межрегионные нарушения ловит re-audit); коммит отложен до конца прохода; часть реальных calque не чинится автоматически (debt; в v4 нет более поздней полировочной фазы). Ожидаемый эффект: переключения ~190→~6, Qwen-токены ~620k→~166k (и ниже с L3). Реализация — без PR/деплоя/запуска пайплайна, с офлайн-реплеем на замороженных вердиктах.

2026-08-03. Реализация L1/L2b/L3 в `pact_v4/phase4/repair.py` (проходы по ролям через `_run_repair_round`, узкий регейт `BackendRegionFidelityGate`/`REGION_FIDELITY_GATE_V1`, фильтр `filter_soft_findings`/`SoftFindingsPolicy`). Уточнения и трейд-оффы, зафиксированные по ходу: (1) **Коммит применяет только target PID'ы правки** — tentative-правки готовятся на едином срезе текста (по одному срезу на чанк), поэтому запись всего чанка в `translation_by_chunk` затирала бы правки соседних регионов того же чанка (обнаружено на офлайн-реплее главы 0001: конвергенция на замороженных вердиктах расходилась ровно из-за этого). Применение только `target_pids` сохраняет все правки чанка; та же семантика и для resume-пути (закэшированные записи применяются по своим target PID'ам). `RepairRecord.new_translation` по-прежнему несёт полный срез чанка (base + собственная правка) — как и у легаси-`repair_region`. (2) **L3-эвристика**: слабое доказательство = короткий непустой excerpt (`0 < len < 60`) И/ИЛИ неуверенная note (маркеры `sounds like`/`might be`/`likely`/`похоже`/`возможно`/`вероятно`/`кажется`/`скорее всего`/`наверное` и др.). Пустой excerpt сам по себе — не слабость. Пороги — параметры `SoftFindingsPolicy`, не магия в коде. (3) **Блокирование раунда 2**: из blocking-находок исключаются именно слабо-доказательные soft-находки (детектор `gemma_russian_review` + soft-категория + слабое доказательство); уверенные soft-находки остаются blocking и ремонтируются во 2-м раунде. (4) **Офлайн-реплей** (`tests/pact_v4/phase4/test_phase4_offline_replay_0001.py`, read-only над артефактами `run_001`) на замороженных вердиктах (репейр-вывод фиксирован, регейты фиксированы, re-audit = закэшированные Step-6 issues): набор закоммиченных `repair_id` у нового пайплайна (L3 выключен) тождественен легаси-интерливленному потоку, debt trace и терминальный статус совпадают. Эффект: на главе 0001 новая реализация ремонтирует те же регионы без full-chunk регейтов; L3 уводит слабо-доказательные soft-находки в debt trace, находки остаются в store.

2026-08-03. Решение по доставке библии в промпты (B7, `docs/plans/V4_B7_BIBLE_AND_CROSS_CHAPTER_TASK_RU.md`). Источник фактов — файл v3 (`book_bible.json`), формат может меняться, главное — сохранить факты. Двухуровневая структура (по прецеденту v3): (1) **книжная библия** (`book_bible.json`) — накопительная на книгу (characters/entities/address_register/facts + индекс глав), начальное наполнение из v3; (2) **главная память** (`chapter_memory`) — frozen снапшот на главу (glossary + book_memory), shadow-наблюдения, `pov.gender` (род рассказчика). Междуглавная аккумуляция: `MemoryManager.promote()` переносит наблюдения в книжную библию при `complete` + при `accepted_degraded` переносит из чанков, которые **не были карантинными** (решение владельца). Book-run wrapper — запуск глав по порядку на общем `--memory-dir`. Рендер библии в промпты: генерация + fidelity-гейт + аудит (закрывает гэп рода рассказчика). Bible-excerpt входит в `PromptBundle.bundle_hash` (смена библии инвалидирует cache/resume).

2026-08-03. Решение по порядку реализации post-run_001 quality refinements (B4–B8, `docs/plans/V4_IMPLEMENTATION_ORDER_PLAN_RU.md` Поток B+). Порядок по зависимостям: (1) **B4** JSON-устойчивость — retry для пустого/обрезанного JSON в qwen-audit и repair (база для всех); (2) **B5** mixed_script-политика — allowlist легитимных латинских инициалов/имён или транслитерация (разблокирует chunk0001); (3) **B6** quarantined-чанки — отдельный цикл ремонта или признание карантинных финальными с best-variant (зависит от B4); (4) **B7** библия + междуглавная + book-run (зависит от B4); (5) **B8** повторный прогон главы 0001 — валидирует B4–B7 + L1/L2b/L3 + tracker (зависит от B5/B6/B7). Альтернатива (параллелизм): B5 и B6 можно делать параллельно после B4 (обе зависят от B4, но не друг от друга). Решение владельца 2026-08-03.

2026-08-02. Phase 5 (B3) formatting policy: the §8.14 span contract is implemented as a **wrap-only** restoration step applied between Step 7 (convergence) and Step 8 (terminal), so the final integrity check and the terminal transition see exactly the text that goes into `complete`. Inline HTML spans (em/strong/i/b/a, from `pact_v4.phase0b.source_html`) are re-located in the translated text and wrapped; formatting never rewrites the visible content, so the Step 8 conditional narrow Qwen smoke (content change outside the Step 7 re-audit scope) is never tripped by formatting alone. Tier cascade with provenance (`pact_v4/phase5/formatting.py`): exact → occurrence-aware (1:1 duplicate assignment, e.g. `p00058`-style repeated `No`) → conservative fuzzy (case/ё-е/quotes/whitespace/hyphen only) → model fallback through the Backend boundary (`BackendFormattingCaller` over the coordinator `CompletionBackend`, `build_formatting_adapters`; never local lifecycle adapters — an import-guard test enforces it). Blocking integrity: every unresolved required span is a blocking incident; production default `max_formatting_incidents=0`, so a single unresolved span prevents `complete` and yields `accepted_degraded` when the PID map stays structurally valid, never `failed` from a formatting transport failure alone ("transport failure ≠ semantic gate failure"); no marker/placeholder can leak into the output. The formatted text is recorded in `repair_report.json` (final_translation) and a dedicated `formatting_report.json` carrying backend identity.

2026-08-02. Результаты live-прогона Step 6 (B1 all-chunks audit, глава 046, `run_006`): прогон — resume (`resumed_from_index=11`) поверх генерации `run_004` (v1, step 6 там `skipped/partial_selection`); в `run_006` отработал именно B1-step 6 (аудит всех чанков, карантинные — best-variant). Итог: `complete`, 11/11 чанков покрыто, `failed_units=[]`, 93 находки / 74 региона (qwen 47, gemma 44, deterministic 2). Карантины чанков 0002/0003 подтверждены как корректные: Qwen-gate отбросил оба кандидата (0002 — мусор `сersет` в p00053 + семантическая инверсия p00053 у BL; 0003 — `neatly`, «Мне не вам об этом говорить», «Правило большого пальца», инверсия «сойти с проторенной тропы» у BL); причина зафиксирована в `run_004/selection_results.json`. Вывод: step 6 полезен — единственный уровень, ловящий сквозные ты/вы, референты и регистр поверх швов чанков (у закоммиченных чанков 4–11 находок, ни один не прошёл чисто), даёт порегиональный план ремонта. Ограничения: это диагностика, а не ремонт — `translations.json` неполный (p00035–p00092, 58 абзацев чанков 2–3 отсутствуют, закрытие за Phase 4); часть находок субъективна (calque/register) — нужен severity-фильтр перед передачей B2; `quarantine_reason` в `b2_handoff.json` для resumed-чанков = `null` (sidecar `selection_meta.json` в `run_004` не писался), причина сохранена только в `run_004/selection_results.json`.

2026-08-02. Зафиксированы результаты двух технических исследований и литературного сравнения `run_006` (локальный, resume `run_004`) с `run_003_remote` (OpenCode) в [`docs/audits/RUN_003_REMOTE_VS_006_STUDY_SUMMARY_RU.md`](docs/audits/RUN_003_REMOTE_VS_006_STUDY_SUMMARY_RU.md). Рабочий вывод для редакторской доводки: базой считать `run_003_remote`, а удачные формулировки `run_006` переносить только точечно после сверки с оригиналом. Это не решение о смене model/gate policy: прогоны выполнены на разных ревизиях/конфигурациях, а удалённый B1-аудит неполон; для сравнения качества обязательны по-PID сверка и воспроизводимый прогон с полным покрытием.

2026-08-02. Production-папка репозитория перенесена: checkout ветки `main` переведён из `D:\pact\pact_translator_v3` в `D:\pact\pact_translator_v4` (v4-папка отслеживает `origin/main`). Причина: владелец хочет, чтобы «деплой» не требовал уточнять отдельную папку — развёртывание идёт в единственную production-папку на `main`. `D:\pact\pact_translator_v3` остаётся primary git-worktree (содержит `.git`) на новой локальной ветке `v3-primary-home`; AGENTS.md обновлён (Production: `D:\pact\pact_translator_v4`).

2026-08-02. Уточнено правило deployment provenance после перехода `main` на V4: `deployment_provenance.v31.json` — архивный record последнего V3-релиза и не должен требоваться, копироваться или сверяться для V4 deployment. Для V4 deployment проверяется точный reviewed tag/HEAD, чистое дерево, остановленный pipeline и backup; provenance V4 валидируется по собственным run artifacts (source/snapshot/chunk-plan/config/backend) при запуске или resume. V3 drift-guard остаётся только для архивных V3-релизов.

2026-08-02. Решение владельца: `main` переведён на v4 (форс-репоинт на коммит 66a6634, совпадает с вычищенной веткой `v4.0`); `v4.0` остаётся алиасом на тот же коммит. v3 больше не используется: его последняя рецензируемая линия (`4cbf958`) сохранена тегом `archive/v3-main-20260802`, локальный незавершённый WIP (`1169118`, правки `pact_translate_v3.py` и `test_v31_finalize_guard.py`) — тегом `archive/v3-main-wip-20260802`. Прод-репозиторий переводится на v4-main одновременно. Это разворот прежнего правила «V3-деплой на main, V4 в отдельных worktree»: v3-линия зафиксирована как архив, разработка и будущее развёртывание идут с `main`.

2026-08-02. Решение владельца по scope аудита Step 6 (B1-followup; реализация самого all-chunks-аудита и `b2_handoff.json` — отдельная карточка, вне этой записи). Step 6 аудирует **все чанки** плана: selected → победитель каскада; карантинные с вариантами → детерминированно выбранный **best-variant** (правило выбора: макс. пройденных гейтов в `decision_trace` → приоритет роли `fidelity_first` → `balanced_literary` → `synthesis` → lexicographic `candidate_id`). Аудит карантинного варианта — **диагностика, не приёмка**: чанк остаётся карантинным, даже если best-variant прошёл аудит чисто; статус чанка несёт handoff, а не `step6.status`. Чанки без вариантов (`needs_synthesis` / `incomplete_generation`) → только `missing`-покрытие, без модельных юнитов. Новый артефакт `b2_handoff.json` (`schema: pact-v4-step6-b2-handoff/v1`) — per-chunk статус/вариант/причина/покрытие как контракт входа B2. Отменяется прежняя граница B1 «аудит только при полном покрытии выбора»: скип `partial_selection` убирается, скип — только при 0 кандидатов.

2026-08-02. **B1-followup (Step 6: аудит всех чанков + handoff для B2) — реализация.** Это разворачивает границу (1) из записи B1 2026-08-01 (`skipped/partial_selection` при неполном покрытии выбора): на главе 046 (run_002/run_004) карантины систематичны (9 selected, 2 quarantined), поэтому полный-выбор-скип означал, что аудит вообще не выполняется — теряются и вычитка закоммиченных чанков, и честная фиксация пробелов. Что изменилось: (1) `AssembledChapter.assemble`/`run_chapter_audit` принимают partial candidate map — чанк без кандидата пропускается модельными треками (Qwen/Gemma unit не создаётся и не вызывается), а deterministic-слой честно помечает его PID'ы `missing` с candidate_id-маркером `"<no-candidate>"`; `chapter_hash` остаётся детерминированным (считается от реального `translation`). (2) В strict-драйвере Step 6 строит карту кандидатов: selected → закоммиченный победитель; quarantined с вариантами → детерминированный **best-variant** (правило, записываемое в артефакт: макс. пройденных гейтов в `decision_trace` → приоритет роли `fidelity_first`→`balanced_literary`→`synthesis` → лексикографический `candidate_id`), пересоздаётся через `Candidate.create`; needs_synthesis / incomplete_generation / непереведённые чанки — только `missing`-покрытие без модельных юнитов. (3) `skipped/partial_selection` убран; скип остался только при **0 кандидатов** (`no_selected_chunks`, defense-in-depth); `incomplete_translation` (tampered prior translations) остаётся отдельным data-integrity failure. (4) Новый артефакт `b2_handoff.json` (schema `pact-v4-step6-b2-handoff/v1`, входа B2): identity chapter/snapshot/plan/config/backend + `chunks[]` с `chunk_id/plan_pids/status(audited|quarantined|needs_synthesis|incomplete_generation)/committed/audited_candidate_id|role/best_variant_rule/available_variants[{candidate_id,role,gates_passed}]/quarantine_reason/gate_trace/uncovered_pids`. Семантика: аудит карантинного best-variant — **диагностика, не приёмка**; чанк остаётся `quarantined` в handoff, даже если best-variant прошёл аудит чисто; per-chunk статус несёт handoff, а не `step6.status`. (5) Step 6 получает `generation_records`; `generation_outcomes.json` теперь мержится кумулятивно через resume (предыдущие сессии + текущая), чтобы карантинные чанки прошлых сессий имели восстанавливаемые варианты — заодно закрывает известный пробел реконструкции `generation_outcomes.json` из записи 2026-08-01. (6) `_load_audit_cache`: mismatch `chapter_hash`/snapshot/plan/config при resume (например, глава выросла: частичный аудит прошлой сессии → более полный в этой) — это свежий cache (все unit-ключи всё равно miss), а не foreign-identity failure; mismatch `backend_identity_hash` остаётся жёстким отказом (backend не входит в unit-hash). Вне scope: repair/convergence/terminal (Phase 4 — B2), formatting (Phase 5 — B3), OpenCode (Поток C), journal v2, изменения гейтов/каскада Phase 1–2.

2026-08-02. **B1-followup — контрактные уточнения по ревью PR #108.** Зафиксированы по комментариям ревью (APPROVE с уточнением до merge): (1) **`audit_status` в `b2_handoff.json`** — per-chunk поле `clean` / `findings_present` / `unit_failed` / `no_candidate`, заполняется после аудита; `uncovered_pids` — **только структурное** покрытие (PID'ы чанка без текста кандидата в собранной главе, всегда пусто у чанка с audited-кандидатом), а полнота аудита несёт именно `audit_status`: selected-чанк с упавшим Qwen/Gemma-юнитом даёт `uncovered_pids=[]` + `audit_status="unit_failed"`, и B2 не должен выводить это из `uncovered_pids`+`committed`. (2) **`quarantine_reason` переживает resume** через новый кумулятивный sidecar `selection_meta.json` (schema `pact-v4-strict-selection-meta/v1`, тот же lifecycle, что у `generation_outcomes.json`): journal v1 не хранит `quarantine_reason`, поэтому без sidecar'а handoff после resume молча давал `quarantine_reason=None`; merge — prior-запись побеждает для resumed-чанка (текущая journal-производная запись — лишь заглушка), текущая сессия перезаписывает. Заодно чинит известный пробел реконструкции `selection_results.json` (DECISIONS 2026-08-01). (3) Именование: `gate_trace` (journal) и `decision_trace` (selection/generation записи) — одно и то же, не «нормализовать» одну сторону без другой. Тесты: 555 passed в `tests/pact_v4`.

2026-08-02. C3 (PR 4 ╨╕╨╜╤é╨╡╨│╤Ç╨░╤å╨╕╨╛╨╜╨╜╨╛╨│╨╛ ╨┐╨╗╨░╨╜╨░, ╨ƒ╨╛╤é╨╛╨║ C): CLI `--runtime-config` + ╨┐╨╛╨┤╨│╨╛╤é╨╛╨▓╨║╨░ live-╨▓╨░╨╗╨╕╨┤╨░╤å╨╕╨╕. ╨Ü╨╛╨┤: `v4_phase12_strict_run.py` ╨┐╨╛╨╗╤â╤ç╨╕╨╗ `--runtime-config <file.yaml/json>` (╨╖╨░╨│╤Ç╤â╨╖╨║╨░ ╤ç╨╡╤Ç╨╡╨╖ `load_runtime_config`; JSON ΓÇö ╨▒╨╡╨╖ ╨╖╨░╨▓╨╕╤ü╨╕╨╝╨╛╤ü╤é╨╡╨╣, YAML ΓÇö ╤ç╨╡╤Ç╨╡╨╖ PyYAML) ╨╕ `--managed-server` (`force_managed` ╤Ç╨╡╨║╤â╤Ç╤ü╨╕╨▓╨╜╨╛ ╨┐╨╡╤Ç╨╡╨║╨╗╤Ä╤ç╨░╨╡╤é ╨║╨░╨╢╨┤╤ï╨╣ `opencode_server` ╤ü╤â╨▒-╨▒╤ì╨║╨╡╨╜╨┤ ╨▓ `server_mode: managed`; ╨▓╨╡╤Ç╤à╨╜╨╡╤â╤Ç╨╛╨▓╨╜╨╡╨▓╤ï╨╣ `local_llama` ╨┐╨╛╨┤ `--managed-server` ΓÇö ╨╛╤ê╨╕╨▒╨║╨░, ╨▓╨╗╨╛╨╢╨╡╨╜╨╜╤ï╨╣ local ╨▓ composite ╨╛╤ü╤é╨░╤æ╤é╤ü╤Å ╨╜╨╡╤é╤Ç╨╛╨╜╤â╤é╤ï╨╝ ΓÇö Pact ╨╕ ╤é╨░╨║ ╨▓╨╗╨░╨┤╨╡╨╡╤é llama-server). ╨æ╨╡╨╖ ╤ä╨╗╨░╨│╨░ CLI ╨┐╨╛╨▒╨░╨╣╤é╨╛╨▓╨╛ ╤Ç╨░╨▓╨╡╨╜ ╨┐╤Ç╨╡╨╢╨╜╨╡╨╝╤â ╨╗╨╛╨║╨░╨╗╤î╨╜╨╛╨╝╤â strict-╨║╨╛╨╝╨░╨╜╨┤╨╡ (╤é╨╛╤é ╨╢╨╡ ╨┐╨╛╤Ç╤Å╨┤╨╛╨║ model calls, lifecycle, record). ╨ö╨╛╨▒╨░╨▓╨╗╨╡╨╜╤ï ╨╝╨╛╤ü╤é╤ï ╤Ç╨╛╨╗╨╡╨╣: `build_role_backend`/`build_role_adapters` (`pact_v4.runtime.runtime_config`) ╤ü╤é╤Ç╨╛╤Å╤é ╨┐╤Å╤é╤î `Backend*`-╨░╨┤╨░╨┐╤é╨╡╤Ç╨╛╨▓ ╨╜╨░╨┤ `CompletionBackend` ╨╕╨╖ ╤ü╨╛╨▒╤Ç╨░╨╜╨╜╨╛╨│╨╛ runtime; `CompositeRuntimeCoordinator` ╨┐╨╛╨╗╤â╤ç╨╕╨╗ ╨╛╨┐╤å╨╕╨╛╨╜╨░╨╗╤î╨╜╤ï╨╣ `backend` (╤é╨╛╤é `CompositeCompletionBackend`, ╤ç╤é╨╛ ╤ü╤é╤Ç╨╛╨╕╨╗ `build_runtime`, ╨╜╨╛ ╨▓╤ï╨▒╤Ç╨░╤ü╤ï╨▓╨░╨╗) ╨╕ ╤ü╨▓╨╛╨╣╤ü╤é╨▓╨╛ `.backend`. ╨ÿ╤ü╨┐╤Ç╨░╨▓╨╗╨╡╨╜ ╨╕╨╜╤é╨╡╨│╤Ç╨░╤å╨╕╨╛╨╜╨╜╤ï╨╣ ╤Ç╨░╨╖╤Ç╤ï╨▓ C1/C2: `BackendModelCaller` ╤Ç╨╡╨╖╨╛╨╗╨▓╨╕╨╗ ╨╝╨╛╨┤╨╡╨╗╤î ╨│╨╡╨╜╨╡╤Ç╨░╤å╨╕╨╕ ╤é╨╛╨╗╤î╨║╨╛ ╨┐╨╛ ╤Ç╨╛╨╗╤Å╨╝ bundle (`fidelity_first`/`balanced_literary`), ╤é╨╛╨│╨┤╨░ ╨║╨░╨║ ╨║╨╛╨╜╤ä╨╕╨│╨╕ ╨┐╨╛ ┬º8 ╤ü╨▓╤Å╨╖╤ï╨▓╨░╤Ä╤é ╨╡╤æ ╨┐╨╛╨┤ ╨░╨╗╨╕╨░╤ü╨╛╨╝ `generator` (╨▓ parity-╤é╨╡╤ü╤é╨░╤à ΓÇö ╨┐╨╛╨┤ `default`); ╤é╨╡╨┐╨╡╤Ç╤î lookup ╨┐╤Ç╨╛╨▒╤â╨╡╤é `(bundle.role, "generator")` ╨╖╨░╤é╨╡╨╝ `default` ΓÇö ╨║╨╛╨╜╤ä╨╕╨│ ╨╕╨╖ ╨┐╨╗╨░╨╜╨░ ╤Ç╨░╨▒╨╛╤é╨░╨╡╤é, ╤ü╤â╤ë╨╡╤ü╤é╨▓╤â╤Ä╤ë╨╕╨╡ ╨║╨╡╨╣╤ü╤ï (default) ╨╜╨╡ ╨╝╨╡╨╜╤Å╤Ä╤é╤ü╤Å. Composite-╨┐╤Ç╨╛╤ä╨╕╨╗╤î ╤é╤Ç╨╡╨▒╤â╨╡╤é ╨┐╨╛╤Ç╤Å╨┤╨║╨░ `opencode` ╨┐╨╡╤Ç╨╡╨┤ `local` ╨▓ `backends` (╨┤╨╡╤ü╨║╤Ç╨╕╨┐╤é╨╛╤Ç╨╜╤ï╨╣ union ╤ü╨▓╤Å╨╖╤ï╨▓╨░╨╡╤é ╤Ç╨╛╨╗╤î ╤ü ╨┐╨╡╤Ç╨▓╤ï╨╝ ╤ü╤â╨▒-╨▒╤ì╨║╨╡╨╜╨┤╨╛╨╝, ╨╡╤æ ╨╛╨▒╤è╤Å╨▓╨╕╨▓╤ê╨╕╨╝) ΓÇö ╨╖╨░╤ä╨╕╨║╤ü╨╕╤Ç╨╛╨▓╨░╨╜╨╛ ╨║╨╛╨╝╨╝╨╡╨╜╤é╨░╤Ç╨╕╨╡╨╝ ╨▓ `configs/runtime_composite.example.yaml`. ╨ƒ╤Ç╨╕╨╝╨╡╤Ç╤ï ╨║╨╛╨╜╤ä╨╕╨│╨╛╨▓ ╨▒╨╡╨╖ ╤ü╨╡╨║╤Ç╨╡╤é╨╛╨▓ ╨▓ `configs/`: local/remote/composite, auth ΓÇö ╤é╨╛╨╗╤î╨║╨╛ env-╨╕╨╝╨╡╨╜╨░ (`username_env`/`password_env`). `_smoke_c3.py` ΓÇö owner-run live one-chunk smoke ╨┐╨╛ ┬º14.5: ╤ü╨╕╨╜╤é╨╡╤é╨╕╤ç╨╡╤ü╨║╨░╤Å ╨│╨╗╨░╨▓╨░ ╨╕╨╖ 1 low-risk + 1 high-risk A/B ╤ç╨░╨╜╨║╨╛╨▓, ╤Å╨▓╨╜╤ï╨╣ ┬º12-ack ╤ü ╨╕╨╜╤é╨╡╤Ç╨░╨║╤é╨╕╨▓╨╜╤ï╨╝ ╨┐╨╛╨┤╤é╨▓╨╡╤Ç╨╢╨┤╨╡╨╜╨╕╨╡╨╝ ╨┤╨╛ ╨┐╨╗╨░╤é╨╜╤ï╤à ╨▓╤ï╨╖╨╛╨▓╨╛╨▓, ╨▓╤ï╨▓╨╛╨┤ usage/provenance ╨╕ request/session ID ╨╕╨╖ record, ╨┐╤Ç╨╛╨▓╨╡╤Ç╨║╨░ ╨╛╤é╤ü╤â╤é╤ü╤é╨▓╨╕╤Å credential values ╨▓ ╨░╤Ç╤é╨╡╤ä╨░╨║╤é╨░╤à. Live-╨▓╤ï╨▓╨╛╨┤╤ï (operational metrics remote/composite vs local chapter_046, quarantine causes, ╤Ç╨╡╤ê╨╡╨╜╨╕╨╡ default-vs-╤ì╨║╤ü╨┐╨╡╤Ç╨╕╨╝╨╡╨╜╤é ╨╕ ╨╝╨░╤Ç╨║╨╡╤Ç ╨╜╨░ blind quality comparison) ΓÇö **╨╖╨░╨┐╨╕╤ü╤ï╨▓╨░╤Ä╤é╤ü╤Å ╨▓╨╗╨░╨┤╨╡╨╗╤î╤å╨╡╨╝ ╨▓ ╤ì╤é╨╛╤é ╤ä╨░╨╣╨╗ ╨┐╨╛╤ü╨╗╨╡ ╨╛╨┐╨╗╨░╤ç╨╕╨▓╨░╨╡╨╝╨╛╨│╨╛ ╨┐╤Ç╨╛╨│╨╛╨╜╨░**; ╨║╨╛╨┤ ╨╕ PR 4 ╨╜╨╡ ╨▓╤ï╨┤╤â╨╝╤ï╨▓╨░╤Ä╤é ╤ì╤é╨╕ ╤Ç╨╡╨╖╤â╨╗╤î╤é╨░╤é╤ï.

2026-08-02. Composite-профиль (C3, ревью PR #109): порядок суб-бэкендов в ackends конфига composite — осознанный v4-компромисс, а не дефект: дескрипторный union связывает роль с первым суб-бэкендом, объявившим её привязку, поэтому opencode обязан идти перед local, иначе роли генератора/fidelity молча маршрутизируются на локальные модели. См. комментарий в configs/runtime_composite.example.yaml. Порядок-независимый union дескриптора и явный per-role 
ole_backend_map (без «first match wins») — задача v5, вне v4 (план §8: v4 не адаптирует topology к backend).

2026-08-02. **B2 (Phase 4A/4A2/4B: minimal repair, Gemma closure, convergence, terminal) — реализация.** Repair-политика зафиксирована в коде (`pact_v4/phase4/repair.py`) по согласованному плану и DECISIONS 2026-08-01/02: (1) Терминальные состояния приведены к спеке `complete / accepted_degraded / failed`; `quarantined` — внутреннее repair-состояние (V4_MVP_SPEC_RU.md §7), не терминальное; переход монотонный (write-once). (2) Repair строго finding-linked через region_resolver; `full_sentence_rewrite` требует documented reason и выбирается только после неудачного `region_edit` (локальная правка показана невозможной); challenge требует evidence и не auto-accept'ится (контракт `models.Repair`). (3) Qwen re-gate failure никогда не коммитит repair; обязательный Gemma re-check только для finding'ов Gemma Russian review (детерминированный слой не может закрыть Russian-only finding); failure re-check оставляет finding открытым и возвращает последний admitted текст как degraded availability. (4) Один обязательный convergence-round с re-audit изменённых PID + discourse-соседей; второй — только при blocking finding или изменённой границе; integrity check по умолчанию детерминированный, узкий Qwen smoke — только при изменениях вне scope Step 7 re-audit (по умолчанию False). (5) `accepted_degraded` — только при валидном структурном PID-map + debt trace, без memory promotion; `failed` — только при отсутствии валидного PID-map. (6) Transport failure / invalid structured output при repair = debt/incomplete, не семантический терминальный статус (transport failure ≠ semantic gate failure); no silent fallback. (7) Двойной режим: все repair-вызовы идут через Backend-адаптеры над coordinator `CompletionBackend` (`build_repair_adapters` = `BackendRepairCaller`/`BackendQwenEvaluator`/`BackendQwenAuditEvaluator`/`BackendGemmaAuditEvaluator`), запрещены локальные Lifecycle-адаптеры/ModelRouter в repair-коде (импорт-guard тест); repair-артефакты несут backend identity, resume переиспользует `repair_cache.json` с foreign-identity проверкой. (8) Step 7/8 встроены в strict-драйвер как `step7`/`step8` в record (Skip без repair-адаптеров для обратной совместимости). Вне B2: Phase 5 formatting (B3), memory promotion (Phase 1B), синтез для no-candidate чанков (Phase 2C).

2026-08-01. Коммит `ba396dd` (strict single-resident driver + real chapter_046 trial) был влит в `v4.0` напрямую, без PR — это отклонение от documented Codex-implements/Claude-reviews-via-PR convention, замеченное пользователем постфактум. Remediation: следующий коммит на ту же тему (Qwen `max_tokens` fix) оформлен как PR #96 с описанием, покрывающим оба коммита, чтобы у ревьюера была точка для комментариев по уже влитому. На будущее: прямой push в `v4.0` для work generated by Claude под explicit override (см. [[v4_phase_roles]] аналог в agent memory) не заменяет PR review — override разрешает Claude писать код, не разрешает пропускать review process.

2026-08-01. По итогам review PR #96 (сторонняя модель) внесены точечные правки без отката ba396dd: (1) `pact_v4/runtime/qwen_evaluator.py` — `max_tokens` для Qwen fidelity gate теперь floor (16384) + динамическая надбавка `128 * len(translation)` с потолком 24576, вместо одного статического числа (chunk_plan.json реального прогона показал разброс 19-47 PID/chunk без структурного потолка со стороны ChunkPlanner); `_parse_qwen_verdict` различает пустой ответ / вероятный truncation / прочий malformed JSON в `detail`, чтобы будущий рецидив был диагностируем по record'у, а не только повторным ручным repro. (2) `pact_v4/pipeline/v4_phase12_strict_runner.py` — `translations.json` пишется инкрементально (atomic write-then-rename) после каждого selected chunk, не только в конце run'а, иначе crash между последним journal entry и финальной записью терял бы уже закоммиченный перевод при resume; foreign-identity check при resume расширен `StrictBackendConfig.identity_hash` (exe/device/model_paths/model_names/server_args), не только snapshot/plan/config; `halt_reason` теперь перечисляет конкретные причины последних non-selection chunk'ов, а не только их число. (3) Добавлен regression-тест на restart-accounting при реально вызванной Gemma preference (2 passing кандидата без disagreement) — предыдущий тест покрывал только low-risk (1 кандидат, preference не нужна). Не сделано в рамках этого review-цикла: full chapter_046 re-run с fix'ом (оставлено оператору с GPU-доступом, см. `v4_diag_qwen_truncation_repro.py`).

2026-08-01. Известный пробел в `run_chapter_strict` (`pact_v4/pipeline/v4_phase12_strict_runner.py`), обнаружен при разборе `run_002`: resume восстанавливает полноту финального отчёта не одинаково для трёх artifact'ов. `translations.json` — полная и корректная реконструкция (фикс B1, PR #96): пишется инкрементально, на resume читается заново, `left_context` для продолжения строится верно. `selection_results.json` — restored только частично: на каждый journal entry из прошлых сессий кладётся урезанная запись (`chunk_id`/`status`/`selected_role`/`resumed: True`), без `quarantine_reason`, `risk_band`, `candidates_produced`, `disagreement_detected/reason`, `decision_trace`, счётчиков `candidates_evaluated/passed/failed` — эти поля есть только для chunk'ов, обработанных в последней (текущей) сессии. `generation_outcomes.json` — не восстанавливается вообще: `generation_records` инициализируется пустым списком и пополняется только в текущей сессии, поэтому в финальном файле после resume отсутствуют кандидаты (переводы A/B) для всех chunk'ов из прошлых сессий. Не является багом в смысле "resume даёт неверный результат" — сам перевод (`translations.json`) и решения (`journal.ndjson`, полный per-chunk источник правды) целы и верны; это пробел в *полноте вторичных human-readable отчётов* после resume, обнаруженный именно потому, что понадобилось достать `quarantine_reason`/кандидатов chunk'а из прошлой сессии `run_001` после её resume. Фикс не сделан — оставлено отдельной карточкой, не блокирует текущий `run_002`.

2026-08-01. Итог обсуждения после `ba396dd`/PR #96/#97: принятие strict single-resident driver'а как default НЕ требует переписывать или архивировать Phase 1/2. `pact_v4/pipeline/v4_phase12_strict_runner.py` импортирует `select_candidate`, `generate_for_chunk`, `ChunkPlanner`, `assess_source_risk` и весь остальной Phase 1/2 код без изменений (сознательное ограничение карточки) — эта логика общая для strict, batch-first и старого interleaved driver'а и не зависит от lifecycle-топологии. Phase 3 (`run_chapter_audit`) тоже не требует переписывания: он принимает уже собранную `AssembledChapter` и финальную selected-карту кандидатов, не имея информации о том, как глава была сгенерирована — агностичен к топологии по конструкции (проверено чтением `pact_v4/phase3/audit.py`). Что реально требует решения при окончательном принятии strict: (1) формальная запись в DECISIONS.md, явно переопределяющая решение от 2026-07-30 ("предпочтительный кандидат — batch-first, strict — oracle/control") — это разворот, а не дополнение, должен опираться на числа Измерений 1/2 и реального `run_001`/`run_002`; (2) `v4_phase12_sequential_runner.py` — у него уже открыт вопрос "archive vs regression fixture до отдельного решения по topology"; принятие strict и есть этот момент; (3) `v4_phase12_draft_runner.py` — не ошибочен, а рассчитан на одновременно резидентные Gemma+Qwen (недоступно на текущем железе); если он перестаёт быть кандидатом в production driver, его роль меняется на reference/fixture, но `v4_phase12_strict_runner.py` сейчас импортирует у него несколько приватных helper-функций (`_left_ru_for_chunk` и др.) — при архивации draft_runner эти хелперы стоит вынести в общий модуль, чтобы strict-runner не зависел от архивного файла; (4) задача 6 (non-inferiority batch-first vs strict) может стать ненужной, если strict принимается как default без approximation (strict сам oracle, сравнивать не с чем) — но это отдельное явное решение, не автоматическое следствие принятия strict. Само решение "strict = default" на 2026-08-01 ещё не принято окончательно — это резюме того, что понадобится сделать, когда/если оно будет принято.

2026-08-01. Находка при разборе restart-стоимости strict-driver'а: если single-resident топология будет принята для Phase 1-2, её ~2N switch'ей на N chunk'ов (видно в live-прогоне `run_002`) НЕ переносятся автоматически на Phase 3 (`pact_v4/phase3/audit.py`, `run_chapter_audit`) в том же порядке величины, хотя Phase 3 тоже вызывает и Qwen, и Gemma на каждый chunk. Причина: в Phase 1-2 per-chunk порядок вынужден — `left_context` chunk'а N+1 обязан быть реальным закоммиченным переводом chunk'а N, поэтому генерация/гейт нельзя пакетировать без потери корректности. В Phase 3 такой межchunk'овой зависимости нет: аудит одного chunk'а (Qwen source↔translation + Gemma Russian-only review) не зависит от результата аудита соседних chunk'ов — вся глава уже собрана (`AssembledChapter`) до начала аудита. Значит батчинг по модели (сначала все Qwen-юниты по всей главе, потом все Gemma-юниты) корректен и даёт ~1-2 switch'а на всю фазу вместо ~2N — без риска для корректности, в отличие от Phase 1-2.

2026-08-01. По итогам внешнего ревью перевода главы 100 (v3) и разбора кода зафиксирован вывод: v3 пропускал блокеры (дрейф рода рассказчика, подмена имени персонажа «Цирюльник», инверсии смысла), потому что ревьюер — та же Gemma, что переводила (`pact_translate_v3.py:30-52`); audit-юниты режут те же трансляционные чанки на батчи 8 PID с контекстом ±2 (`make_audit_units`, `pact_translate_v3.py:2220-2237`) — оба конца шва никогда не видны одновременно; голос/термины не замораживаются на главу; а детерминированные слои узкие (`narrator_gender_check` — whitelist глаголов, `entity_check` пропускает строчные термины через `source[:1].isupper()`). Оценка v4: инверсии/референты/термины закрываются структурно (независимый Qwen semantic admission + дискурс-план + полноглавые детерминированные проверки `pact_v4/phase3/audit.py:352-408`), но род рассказчика и русский регистр остаются на коррелированном Gemma-ревью и §10 Q5 ablation. Предложения по усилению (детерминированный морфологический gender-скан, модовая терминологическая проверка без лока, cross-name substitution, орфоскан словарём, seam-pair audit units, Qwen как второй RU-ревьюер) записаны в `docs/plans/V4_WEAK_SPOT_STRENGTHENING_RU.md` как design-note; реализация — отдельными карточками после Q5. Это не решение «что реализовывать», а фиксация вывода и кандидатных мер.

2026-08-01. По итогам сравнения `v4_phase12_strict_046/run_001` и полного `run_002` зафиксированы эксплуатационные выводы strict single-resident benchmark: входные source/snapshot/chunk-plan/config identities совпали; `run_002` обработал все 11 chunks с 9 selected, 2 quarantined, 0 needs_synthesis и 0 incomplete generation, но сравнение с `run_001` не является чистым A/B, поскольку `run_001` был возобновлён с `resumed_from_index=5` и имел восстановленные решения для первых пяти chunks. На 23 startup (22 model restart) накопительного ухудшения lifecycle не обнаружено: Gemma cold acquire median 15.82 s (12 загрузок), Qwen 18.65 s (11 загрузок), unload median 6.74/6.33 s; значения от первой к последней загрузке не растут. По server timing logs также нет регрессии после повторных reload: Qwen prompt throughput остаётся около 295 tokens/s, eval — около 20 tokens/s; у Gemma prompt throughput не падает монотонно, eval колеблется без устойчивого нисходящего тренда. Это operational measurement, а не доказательство литературного качества или основание принимать translation output без обычных quality gates. Повторяющиеся `ctx_other`/fit warnings и 11 сообщений `n_gpu_layers already set by user to 99` не привели к retry, OOM, early halt или росту reload time; их следует сохранять видимыми для будущего runtime benchmark.

2026-08-01. Владельцем утверждён план порядка реализации v4: `docs/plans/V4_IMPLEMENTATION_ORDER_PLAN_RU.md`. План объединяет согласованный фазовый план (`docs/architecture/V4_FINAL_REVIEW_AND_IMPLEMENTATION_PLAN_RU_v2.md`, Phase 0–7) и интеграцию внешних моделей через OpenCode (`docs/plans/V4_OPENCODE_REMOTE_MODELS_INTEGRATION_PLAN_RU.md`, PR 1–4) в три workstream-потока: A — provider boundary (PR1 интеграционного плана, первый, чистый рефакторинг без изменения поведения); B — достройка quality engine (Phase 3B → драйвер, Phase 4A/4A2/4B repair/convergence/terminal, Phase 5 formatting alignment); C — OpenCode (PR2 клиент + fake-server contract suite, PR3 tagged configs/RuntimeCoordinator/journal v2/resume identity, PR4 CLI `--runtime-config` + live smoke + chapter_046 remote trial); D — финал (Phase 6 operations, Phase 0D non-inferiority policy, Phase 7 A/B release decision). OpenCode-интеграция не заменяет согласованный фазовый план; задачи вне плана — только по решению владельца. Агентам предписано следовать плану (AGENTS.md, раздел «V4 roadmap»).

2026-08-01. Владельцем на основании gate-тестов принято решение выбрать strict-архитектуру (strict single-resident driver, `pact_v4/pipeline/v4_phase12_strict_runner.py`) как производственную архитектуру v4. Это фиксирует выбор, ранее оставленный открытым (записи 2026-07-30/2026-08-01 о batch-first как предпочтительном кандидате и strict как oracle/control): strict становится целевой production-топологией v4; batch-first и sequential остаются экспериментальными/measurement-инструментами до отдельного non-inferiority benchmark'а. Phase 3B (assembled-chapter audit) встраивается в strict-драйвер (Step 6), Phase 4 repair/convergence строится поверх strict.

2026-08-01. Реализация B1 (Phase 3B runtime audit evaluators + Step 6 в strict-драйвере) фиксирует три границы. (1) Step 6 аудит выполняется только при полном покрытии выбора chunk'ов (`selected_chunks == total_chunks`): `AssembledChapter`/`run_chapter_audit` требуют winning-candidate на каждый chunk плана, а strict-драйвер коммитит только selected; при частичном выборе (halted early / quarantined / needs_synthesis) аудит записывается как `skipped/partial_selection` — закрытие пробелов это Phase 4 (repair/convergence), вне B1; при selected-но-неполном тексте chunk'а (tampered/partial prior translations) — `skipped/incomplete_translation` (явная проверка `set(text) != set(chunk.pids)` до `Candidate.create`). (2) `AuditCache`/findings персистятся в отдельные артефакты (`audit_cache.json`, `audit_findings.json` с foreign-identity проверкой по chapter/snapshot/plan/config/backend); журнал остаётся v1 (journal v2 — Поток C). Падение процесса посреди аудита теряет in-memory cache — resume безопасно перезапускает все unit'ы (детерминированно те же findings), это приемлемая цена для B1. (3) Backend-аудит-адаптеры (`BackendQwenAuditEvaluator`/`BackendGemmaAuditEvaluator`) при транспортном отказе ре-райзят `CompletionError`, который `run_chapter_audit` превращает в failed unit — аудит никогда не claim'ит `complete` при модельном отказе. `reasoning=0` для аудита соблюдается тем же способом, что у gate-оценок: не передаётся `enable_thinking`/`request_options` (LocalOpenAIBackend отвергает любые `request_options`), а не отдельным полем запроса.

2026-08-01. Уточнение по итогам ревью PR #102 (замечание о `reasoning=0`): гарантия «аудит/gate-оценки не включают thinking» в B1 держится на двух стечениях — (а) адаптеры не шлют `request_options`, а `LocalOpenAIBackend` отвергает любые `request_options`, (б) library-level Phase 2B не эмитит `enable_thinking`. Это корректно для локального `llama-server`, но **не является контрактным инвариантом**: `OpenCodeServerBackend` (Поток C, PR 2/3) будет честно применять `request_options`, и адаптер B1 молча не пошлёт `reasoning=0`. Решение: не форсировать `request_options={"reasoning": 0}` в B1 (это сломало бы локальный бэкенд и не сработало бы для удалённых моделей, которые не умеют отключать reasoning — например, deepseek). Требование «reasoning=0 для локальных моделей» и «per-model handling для удалённых (некоторые не умеют выключить)» фиксируется как known-architectural-debt на Поток C, где появится модель-агрегирующий `request_options` с учётом возможностей конкретной модели. До тех пор B1-поведение (не шлём `request_options`) остаётся корректным для production-топологии (локальный strict-драйвер).

2026-08-01. A2 (runner decoupling): приватные хелперы `_glossary_entries`, `_left_ru_for_chunk`, `_risk_for_chunk`, `_record_selection`, `_serialize_generation_outcome` перенесены без изменения сигнатур/поведения из `pact_v4/pipeline/v4_phase12_draft_runner.py` в общий модуль `pact_v4/pipeline/_shared_runner_helpers.py`; `v4_phase12_strict_runner.py` и `v4_phase12_sequential_runner.py` больше не импортируют их из draft_runner (защищено grep-ассертом в тесте). Статусы зафиксированы: `v4_phase12_draft_runner.py` — reference/fixture (рассчитан на одновременно резидентные Gemma+Qwen, недоступные на текущем железе, не кандидат в production driver); `v4_phase12_sequential_runner.py` — вопрос "archive vs regression fixture" остаётся открытым до отдельного решения по topology, но он не production driver. Следствие: архивация любого runner-модуля не ломает strict/sequential runner — зависимости от приватных функций архивного файла больше нет.

2026-08-01. C1 (OpenCodeServerBackend, PR 2 интеграционного плана): контракт привязан к версии `opencode serve` **1.4.7** и сверен с тремя источниками (generated SDK types tag v1.4.7, server source tag v1.4.7 — `session/prompt.ts`/`message-v2.ts`/`llm.ts`, live probe), не только с web-документацией — по плану §7.1. Версионные находки, зафиксированные в коде и contract tests: (1) в body `POST /session/{id}/message` v1.4.7 **нет per-request полей** temperature/max_output_tokens/reasoning — температура и max tokens записываются в backend identity, но не отправляются; непустые `request_options` отвергаются громко, а не молча игнорируются (уточнение к записи про `reasoning=0`: «честно применять request_options» на v1.4.7 означает «отвергнуть, если транспорт не может их выразить», а не «молча выбросить»). (2) `format: {type:"json_schema", retryCount}` сервер принимает, но выполняет единственную попытку (`StructuredOutputError` с `retries:0`), поэтому bounded structured-output retry реализован на стороне клиента в пределах бюджета retry-запросов. (3) `tools` в body сообщения v1.4.7 действительно отключает инструменты для модели (`LLM.resolveTools` фильтрует по `user.tools[k] !== false`), поэтому все-disabled-карта строится по `/experimental/tool/ids` и проверяется в fake-server тестах. Backend использует отдельную session на work unit, `close()` удаляет только свои session'ы по policy, и ни один артефакт/log не содержит Basic Auth credentials. Вне scope C1 остаются RuntimeCoordinator/tagged configs/journal v2 (C2) и managed `opencode serve` (C2, маркер в плане).

2026-08-01. По итогам ревью PR #105 (C1): (1) B1 — `temperature`/`max_output_tokens` (effective sampling, план §5.4) добавлены в `OpenCodeServerBackendConfig` (`default_temperature`/`default_max_output_tokens`) и входят в `BackendDescriptor.effective_options` → identity: смена sampling инвалидирует cache/resume, даже если wire-контракт v1.4.7 их не передаёт. При этом вскрылся латентный баг A1-boundary: `backend_protocol._is_secret_name` использовал substring-матчинг `"token" in name`, из-за чего `max_output_tokens`/`input_tokens` вычищались из identity как credentials. Исправлено: `token` матчится только как отдельное слово (singular, `api_token`/`auth_token` — secrets), plural `*_tokens` — sampling/usage-поля и остаются в identity; regression-тесты в `test_backend_protocol.py`. (2) S1 — `request_options`-rejection и прочие request-уровневые несовместимости получили отдельный error class `ERROR_REQUEST_NOT_SUPPORTED` (не маскируются как network). (3) S2 — `json_schema` mode без `response_schema` теперь fail-loudly до network-вызова. (4) S4 — runtime-проверка `request.model_ref` против `model_bindings` (как у `LocalOpenAIBackend`), invariant-тест. (5) S5 — явная проверка, что `/session` ответ — объект. (6) S6 — preflight-cache тест дополнительно покрывает `/experimental/tool/ids`. S3 (NoReturn на `_raise_final`/`_raise_budget_exhausted`, убран `raise AssertionError("unreachable")`) и S7 (live smoke до merge в v4.0 — зафиксировать дату/исполнителя/результат в PR description) приняты.

2026-08-01. Источник для Qwen fidelity gate (Phase 2C, `pact_v4/phase2/cascade.py` `select_candidate`) скоупится до PID-набора чанка, а не передаётся целиком по главе. Причина: при полном source (353 PID главы) против chunk-перевода (~34 PID) нарушается контракт ревьюера ("SOURCE и TRANSLATION, same PIDs in the same order", QWEN_FIDELITY_V1), и Qwen честно ставит `completeness=False` на каждом чанке — в реальном `run_003` главы 046 это дало 3/3 quarantined и halted early по operational policy (та же объекция была на 2 chunk'ах `run_002`; разброс selected 9 vs 0 — стохастика модели: иногда Qwen игнорирует расхождение PID-наборов, иногда жёстко проверяет). Полнота покрытия чанка при этом не теряется: generation (`_validate_pid_map`, PID_MISMATCH) уже гарантирует, что кандидат покрывает ровно owned PIDs своего чанка, поэтому chunk-scoped completeness остаётся содержательной sanity-проверкой, а не единственным стражем покрытия. Не является разворотом решений — это фикс контракта вызова гейта, общий для strict/sequential/draft runner'ов.

2026-08-01. Версия opencode-сервера для V4-прогонов (решение владельца, Поток C): PACT использует **собственный пин 1.4.7** и поднимает свой `opencode serve` через `npx -y opencode-ai@1.4.7 serve` на отдельном порту — независимо от того, какой пин использует Vibe Kanban (в его бинаре `vibe-kanban.exe` зашит `opencode-ai@1.4.7`, но это его внутренняя деталь, к PACT-прогонам отношения не имеет). Адаптер `OpenCodeServerBackend` уже версион-независим: версия и policy задаются конфигом (`pinned_server_version`, `server_version_policy`), дефолт — 1.4.7 (контракт сверен с тремя источниками, см. запись C1). Перепин на более новую версию (например, глобальную 1.18.9) — отдельная задача: требует повторной сверки контракта (SDK types, `/doc`, фикстуры offline-тестов) по §7.1.

2026-08-01. C2 (PR 3 интеграционного плана, Поток C): строгий драйвер стал backend-независимым. `run_chapter_strict` наблюдает за lifecycle через `RuntimeCoordinator` (§9.1) вместо прямых `router.switches`/`router.release()`: `LocalLifecycleCoordinator` адаптирует `ModelRouter`, `RemoteRuntimeCoordinator` агрегирует call usage, `CompositeRuntimeCoordinator` комбинирует. Введены tagged конфиги (`LocalLlamaBackendConfig` = старый `StrictBackendConfig` как alias, `OpenCodeBackendConfig`, `CompositeBackendConfig`) с общим интерфейсом `identity_hash`/`public_record()`/`build_descriptor()`/`build_runtime()`/`acceptable_identity_hashes()`; `StrictRunConfig.backend` типизирован общим протоколом. Journal/record v2 (`pact-v4-strict-chapter-trial-journal/v2`, `pact-v4-strict-chapter-trial/v2`): journal хранит `backend_event_indices` и `switch_indices` (для remote `switch_indices` пуст), record получает блоки `backend` (descriptor public_record) и `runtime` (`local_lifecycle: null` для remote, `remote_calls` c usage/cost — `null`, если provider не отчитался); legacy-блок `lifecycle` сохранён для старых ридеров. Resume-identity (§11): локальный `identity_hash` стал дескрипторным (надмножество старого), старый hash сохранён как `legacy_identity_hash` и принимается при resume — старые v1-журналы продолжают возобновляться; remote-identity включает model bindings/structured-output/agent/retry/endpoint, но не credentials — смена API key не инвалидирует resume, смена модели/политики — обязана. Managed-режим: новый `opencode_server_lifecycle.py` поднимает собственный `opencode serve --pure` через `npx -y opencode-ai@1.4.7` на отдельном порту, `assert_port_free_or_owned` — fail-fast на чужом порту, эфемерные basic-auth креды генерируются и кладутся в env subprocess'а (и используются backend'ом), `close()` останавливает только свой процесс. Отклонение от §9.1 в одну деталь: в `RuntimeCoordinator` добавлен `release()` (не входящий в список методов плана) — между Phase 1-2 и Step 6 драйверу нужен *нетерминальный* release (для local — освободить resident-модель, для remote — no-op), потому что `close()` для удалённого backend терминален (закрывает HTTP session), а Step 6 использует тот же backend. Scope: CLI `--runtime-config` и live-прогоны — C3 (PR 4); Phase 1/2, prompts, cascade, risk не менялись.

2026-07-31. После adversarial review batch-first получает шесть исполнимых ограничений. Findings не удаляются: repair помечает evidence audit units с изменившимся context hash как `context_stale_by_repair`, затем создаёт revalidation/supersession records. `accepted_degraded` не promote'ит memory, но создаёт только source-grounded advisory continuity overlay с обязательной revalidation и versioned degraded-chain escalation. «Batch» означает один model lease из bounded work units, а не giant prompt. Qwen gate failure исключает selection/commit; Gemma fallback выбирает только из уже admitted кандидатов, а не закрытый Gemma finding остаётся debt. Batch-first promotion требует pre-registered non-inferiority policy (sample, blind rubric, one-sided margins; integrity margin=0). `v4_phase12_sequential_runner.py` остаётся test-only measurement harness без runtime commit/import до отдельного решения о его archive/regression status.

2026-07-30. ChunkPlan (pact_v4/phase1/models.py) переведён с PID-based hard cap (MIN_PIDS=8/MAX_PIDS=20) на word-based (MIN_WORDS=280/MAX_WORDS=640), это отменяет предыдущее решение зафиксировать 8-20 PID как структурный инвариант. Причина: docs/plans/V4_PHASE_0C_GATE_NOTE_RU.md §1 фиксирует small chunk profile Phase 0C Gate как word-based (target_words=450, min_words=280, max_words=640) и явно документирует, что тот же baseline-профиль на практике дал 16-32 PID/чанк (mean 25.21) — то есть PID-based cap=20 систематически конфликтует с собственным Gate-профилем. Правка PID-cap затрагивала models.py, хотя карточка Work 1 изначально ограничивала diff файлом pact_v4/phase1/chunker.py + тесты — расширение согласовано явно (не тихая правка вне заявленного scope). ChunkPlan получил обязательное поле total_words (сумма word_count исходных PID чанка, вычисляется в chunker.py, а не в модели — ChunkPlan сам не хранит текст source). pact_v4/phase1/chunker.py (ChunkPlanner) переведён на word-based partitioning: DEFAULT_TARGET_WORDS=450, DEFAULT_MIN_WORDS=280, DEFAULT_MAX_WORDS=640, DEFAULT_FOLLOWING_BLOCKS=0 (following_blocks — переименованный context_right_count, right context остаётся допустимой опцией, не default). Тай-брейк между равнозначными natural breaks сохранён как в PID-версии (приоритет наибольшему чанку, ближе к max_words), а не к target_words — target_words используется только как валидируемая граница профиля (min_words <= target_words <= max_words), не как эвристика выбора разрыва: это сознательно узкое решение, чтобы не менять поведение алгоритма сверх необходимого. docs/schemas/v4_chunkplan.schema.json обновлён синхронно (total_words — required integer, hard cap описан в словах, не в PID).

2026-07-30. Phase 0C Track A remains operator-run, but its missing setup/import boundary is now executable: a preparation CLI creates four isolated, non-overwriting config/state snapshots, while the read-only baseline CLI imports completed cell drafts plus manifests and derives actual PID-per-chunk metrics. Model lifecycle remains outside the harness so it cannot attach to or stop a foreign `llama-server`.

2026-07-30. V4 Phase 0C Gate — Gate-only PR, не интеграция в runtime. Phase 1C/2A/2B/2C consumers (pact_v4/phase1/chunker.py, pact_v4/phase2/risk.py, pact_v4/phase2/cascade.py и др.) существуют в отдельных V4 ветках, но НЕ в main и НЕ в codex/v4-phase0c-gate-bench; интеграция policy в эти consumers идёт отдельными тематическими PR (Phase 1C PR, Phase 2 PR), как требует «один этап — один PR». По результатам Track A (57 accepted PID, 0 gaps, FP-candidate 9/57=15.79% во всех 4 ячейках) Gate фиксирует small chunk profile (`target=450/min=280/max=640/following_blocks=0`) как **initial/default** для дальнейшей V4 разработки — потому что Track A **не выявил** измеримого FP-candidate-rate преимущества `rc_on` над `rc_off`. Это **не** заявление «rc_off доказанно лучше» — Track A не измерял quality, performance (time/tokens/reloads) и discourse-когерентность стыков; right context остаётся допустимой будущей опцией, решение пересматривается отдельным benchmark'ом. `number_word` и `tone_profanity` остаются required risk/gate categories. Producer `v4_phase0c_baseline.py` теперь (а) при `monitor=FAILED` + `primary_complete` автоматически заполняет `track_b.terminal_discrepancy={detected, monitor_status, artifacts_say, reason}` и `track_b.notes[]`; (б) `residual_errors.final_residual_total` переводится в typed форму `{status, value_numeric: int|null, reason}` — `value_numeric` равен `len(residual_lifecycle resolved_retry_exhausted)` при residual_complete, иначе `null` с явной `reason`. Schema `pact-v4-phase0c-result-record/v1` делает `track_b.notes` и `track_b.terminal_discrepancy` required, `residual_errors.final_residual_total` — typed через `typed_residual_total` $ref с `additionalProperties: false`. Live baseline record в `D:\pact\gate_bench_runs\phase0c_track_a_001\phase0c_result.json` остаётся **неизменным** до отдельного approved re-issue: переиздание = запись в persistent memory Phase 0C, требует явного «approved». PR не правит v3 production code, run artifacts, translated chapters, cache, Phase 1C/2 consumers.

2026-07-30. Phase 1C → 2A → 2B → 2C driver: Phase 2B (generation) и Phase 2C (cascaded selection) выполняются **в одном per-chunk цикле**, не в двух отдельных проходах. Причина: chunk N+1's `left_context` (Russian text, used as read-only context in the generation prompt) должен браться только из cascade-выбранного кандидата chunk N, а не из `outcome.candidates[expected_roles[0]]` (это просто первый role из generation, до cascade'а). Cascade-контракт "no least-bad selection" применяется симметрично к context propagation: chunk, который не был selected (quarantined / needs_synthesis / incomplete_generation), не имеет established translation — подавать его `fidelity_first` draft в prompt следующего чанка = тихий fallback, который cascade отказывается делать на этапе selection. Альтернативы (pre-pass по selection, отдельный "selected_text_by_chunk" обновляемый после cascade) не дают той же инвариантности: pre-pass требует ещё одного прохода по chunk'ам, а двухфазный driver требует либо подавать неправильный текст, либо решать "что есть selected translation" до cascade'а. Per-chunk interleaving — единственный вариант, который держит инвариант "left_context = previous chunk's selected translation, или () если previous chunk не selected" буквально. Зафиксировано в `pact_v4/pipeline/v4_phase12_draft_runner.py:run_chapter` docstring.

2026-07-30. Для однo-GPU железа (Gemma ~17GB + Qwen ~22GB не помещаются в память одновременно) добавлен параллельный двухпроходный driver `pact_v4/pipeline/v4_phase12_sequential_runner.py` (`run_generate` / `run_select`), НЕ заменяющий interleaved driver из записи выше. На проходе `generate` инвариант "left_context = previous chunk's selected translation" физически невозможен — cascade ещё не выполнялся. Осознанное отклонение: `left_context` берётся из `fidelity_first` DRAFT предыдущего чанка (первый generation-кандидат, до cascade), а не из cascade-выбранного текста. Это ровно тот "original bug"/silent fallback, который `_left_ru_for_chunk` в interleaved driver'е explicitly отвергает — здесь он допускается только как measurement approximation для v3/v4 benchmark gate на слабом железе, зафиксирован как `sequential_model_caveat` в `generation_bundle.json` и `provenance.json`, и не должен использоваться для прод-качества перевода. `run_select` (Phase 2C) остаётся order-independent и корректным без изменений: `select_candidate` зависит только от статического `source` и кандидатов одного чанка.

2026-07-30. `number_word` и `tone_profanity` зафиксированы как required risk/gate категории для V4 (Phase 0C Gate policy, docs/plans/V4_PHASE_0C_GATE_NOTE_RU.md §2). Source of truth — `pact_v4/phase2/risk.py`: `REQUIRED_RISK_CATEGORIES = frozenset({"number_word", "tone_profanity"})`, и `RiskPolicy.__post_init__` отказывается конструироваться, если хотя бы одна из этих категорий отсутствует в `weights` — так требование проверяется на уровне типа, а не только соглашением. Обе категории детектируются source-only (Phase 2A не видит кандидатов/target): `number_word` — written-out cardinal number words в английском источнике (по аналогии с v3's `missing_written_number_words`, но не импортирует из v3 production кода — Phase 2A самодостаточен); `tone_profanity` — источник содержит сильную ненормативную лексику. v3's `pact_translate_v3.py` реализует эквивалентные категории как target-vs-source сравнение (post-generation deterministic gate); Phase 2A pre-screen делает только presence-in-source триггер, поскольку target ещё не существует на этой стадии.

2026-07-30. Phase 2B closure (`feat/v4-phase-2b-ab-generation-rebased`): `pact_v4/phase2/generation.py` and `pact_v4/phase2/prompts.py` propagate `REQUIRED_RISK_CATEGORIES` (imported from `pact_v4/phase2/risk.py`, never redeclared) from the source risk pre-screen into the actual generation prompt. `PromptBundle` gained a `required_risk_feature_codes: Tuple[str, ...]` field — the intersection of the chunk's `RiskAssessment.features` codes with `REQUIRED_RISK_CATEGORIES`, computed in `generation.py` and validated (subset check) in `PromptBundle.__post_init__`; it is part of the hashed bundle identity (`artifact: pact-v4-prompt-bundle/v3`), so a chunk whose flagged required categories differ is a cache miss. `prompts.py` holds `_REQUIRED_CATEGORY_INSTRUCTIONS`, a version-controlled mapping from each required category to an explicit instruction string ("Preserve written-out numbers exactly..." for `number_word`, "Preserve source profanity/tone exactly..." for `tone_profanity`); a module-load-time assertion keeps its key set in sync with `risk.REQUIRED_RISK_CATEGORIES`. `render_prompt` appends a `REQUIRED_CATEGORY_INSTRUCTIONS` block to the rendered request only when at least one required category was actually flagged for that chunk — propagation is conditional on the real pre-screen result, not unconditional, and a plain digit `numbers` feature (not the written-out `number_word` category) never triggers it. Temperature, seed, model, and `GenerationParams.reasoning=0` are unchanged from Phase 1A/2A defaults; no v3 production code (`pact_translate_v3.py` or otherwise) was touched. Phase 2C (`vk/e38a-v4-phase-2c-rebased`) is expected to consume `REQUIRED_RISK_CATEGORIES` from `risk.py` the same way, not redeclare it.

2026-07-30. Phase 2C closure (`vk/e38a-v4-phase-2c-rebased`): `pact_v4/phase2/cascade.py` gains the required-risk-category resolution gate. `required_category_gate(*, risk: RiskAssessment, candidate: Candidate) -> RequiredCategoryGateResult` imports `REQUIRED_RISK_CATEGORIES` from `pact_v4/phase2/risk.py` (never redeclares it) and checks, for each required category the chunk's source `RiskAssessment.features` actually flagged, whether it survived into the candidate's final translation: `number_word` is resolved if the number's value survives as a digit or a matching Russian number word (`_EN_NUMBER_WORD_VALUES` + the `RU_DIGIT_EQUIVALENTS` table, now public in `pact_v4/_integrity_checks.py`); `tone_profanity` is resolved if the translation still contains a comparable Russian strong-register marker (`_RU_PROFANITY_RE`) — a euphemism or silent omission leaves it unresolved. `RequiredCategoryGateResult` is deliberately a distinct shape (`clean: bool`, `unresolved_required: Tuple[str, ...]`) from `pact_v4.phase1.models.GateResult`, which this module already imports for the Qwen/deterministic gates. `select_candidate` gained an optional `risk: RiskAssessment | None = None` keyword — when supplied, a candidate that passed Qwen and `deterministic_consistency_gate` still fails (never selected as final, never "least bad") if `required_category_gate(...).clean` is `False`; the gate result is recorded in `decision_trace` as `GateResult(gate="required_risk_categories", ...)`. `risk=None` (the default) skips the stage entirely, so every pre-existing `select_candidate` caller in this branch is unaffected. Refreshing this branch onto `v4.0` (which had meanwhile absorbed both Phase 2B and a `pact_v4._integrity_checks` extraction of `deterministic_consistency_gate`'s helpers, shared with `pact_v4.phase3.audit`) surfaced that this gate's own `_RU_DIGIT_EQUIVALENTS` table had moved out from under it; resolved by exporting it publicly as `RU_DIGIT_EQUIVALENTS` from `_integrity_checks.py` and importing it, rather than re-declaring a second copy of the same table. No v3 production code touched, no temperature/seed/model changes.

2026-07-30. Follow-up после ревью PR #89 (адресован в merge commit 397fc49 на этой же ветке): `total_words` переведён из caller-supplied int в производное поле (`init=False`), вычисляемое из нового обязательного `word_counts` (по одной записи на PID) — иначе hard cap обходился произвольным несвязанным с `pids` числом. Одиночный leaf-блок, чей собственный `word_count` уже превышает `max_words`, теперь отклоняется на этапе планирования с явной `ValueError` вместо тихого принятия chunker'ом и последующего менее информативного падения в `ChunkPlan`. `ChunkPlanner.__init__` проверяет `min_words`/`max_words` против контрактных границ `ChunkPlan` сразу при конструировании.

2026-07-30. Интеграция PR #89 (Phase 1C word-based chunk sizing) в `v4.0`: на момент мерджа в `v4.0` уже был влит Phase 2B/2C/pipeline driver код (PR #91-93), построенный на старом PID-based `ChunkPlan`/`ChunkPlanner` API — параллельно с ревью Phase 1C. Прямой merge ломал production pipeline (`pact_v4/pipeline/v4_phase12_draft_runner.py` и `v4_phase12_sequential_runner.py` вызывали `ChunkPlanner(min_size=..., max_size=...)`, которых больше не существует) и несколько тестовых фикстур Phase 2/3, конструирующих `ChunkPlan(...)` напрямую без `word_counts`. Исправлено в этом же merge: оба pipeline runner'а переведены на `min_chunk_words`/`target_chunk_words`/`max_chunk_words`, `ChunkPlanArtifact.to_payload`/`from_payload` (models.py) теперь переносят `word_counts` при round-trip, `docs/schemas/v4_chunk_plan_artifact.schema.json` обновлена (добавлен required `word_counts`, снят устаревший `maxItems: 20` на `pids` — это был старый PID-based hard cap, зашитый в wire-schema; реальный cap теперь `ChunkPlan.MAX_WORDS`). Тестовые HTML-фикстуры в `tests/pact_v4/pipeline/` переписаны с контролем реального числа слов на параграф (старый филлер "Plain sentence number N." был ~4 слова/параграф, что ниже фиксированного пола `ChunkPlan.MIN_WORDS=280`); границы чанков в тестах пересчитаны под word-based бюджет, чтобы воспроизвести те же структурные разбиения, что давали старые PID-based фикстуры. Полный набор тестов `v4.0`: 382 passed.

2026-07-30. Для финальной v4-оркестрации на одном GPU принят design baseline: `left_context` следующего chunk'а сохраняет смысл только как cascade-committed перевод предыдущего chunk'а (или `()` после explicit non-selection). Поэтому single-resident production driver должен завершать generation и весь cascade chunk `N` до generation `N+1`, переключая Gemma/Qwen через lifecycle adapter с journal/provenance parent context. Двухпроходный sequential driver с fidelity-first draft остаётся только gate-bench approximation; batching без fixed-point rollback не является допустимым production компромиссом. Реализацию разрешает отдельный measurement gate: измерить стоимость реальных reloads, speculative convergence и sensitivity к left-context.

2026-07-30. Уточнение design baseline после review: `()` после `quarantined`, `needs_synthesis`, incomplete generation или Qwen gate failure — обязательное explicit состояние `empty_after_nonselection`, а не допустимый выбор между пустым context и fidelity-first draft. Причина: draft не прошёл cascade и не является established translation; его подстановка скрыто отменяет no-least-bad contract. При retry, изменяющем non-selection на selection, journal инвалидирует этот chunk и весь suffix, зависимый от его context. Gemma preference может разделять post-Qwen lease с generation следующего chunk только после durable commit текущего chunk; Qwen failure никогда не даёт commit без fidelity gate.

2026-07-30. Scope финального single-resident v4 driver уточнён: только одна локальная машина и ровно один запущенный `llama-server` в единицу времени; второй GPU, удалённая модель и второй service исключены. Для 10 high-risk chunk'ов strict in-order cascade требует 20 model-switch restart (21 startup с начальным Gemma). Единственный допустимый путь уменьшить restart без ослабления exact `left_context` — speculative fixed point с durable prefix commits: 2…20 restart при convergence за 1…10 волн; чистый batching/sequential остаются benchmark-only, поскольку используют unverified draft.

2026-07-30. Текущий двухфазный `v4_phase12_sequential_run` признан допустимым только read-only first-wave measurement: его generation bundle можно повторно использовать для расчёта draft-vs-gate outcome и размера первого invalidated suffix, но select без одновременной Gemma preference применяет deterministic tie-break и не является полным cascade winner. Полная оценка winner требует отдельного shadow re-selection с повторным Qwen gate и последующим Gemma preference, без повторной generation и без изменения исходных run artifacts. Один Gemma→Qwen swap измеряет лишь lifecycle sample, а не Vulkan-деградацию от 20 strict restart.

2026-07-30. После review ограничений single-llama Vulkan lifecycle design preference меняется: strict in-order с exact RU left-context на каждом primary generation остаётся quality oracle, но не default topology. Предпочтительный кандидат — batch-first discourse plan: Qwen готовит frozen source-side handoff constraints, Gemma пакетно генерирует A/B, Qwen пакетно делает semantic admission, Gemma без EN выбирает связную русскую цепочку; exact selected RU context применяется только при targeted boundary repair. Phase 1–2 не дублируют full audit, а гарантируют structural/semantic admission. Phase 3 по умолчанию видит full central chunk и bounded RU excerpts соседей; три full chunk — только risk escalation после benchmark. Один repair-round обязателен, второй только по recorded trigger. Для полностью автоматического выпуска после bounded repair допускается `accepted_degraded` с debt trace и без memory promotion; тихий least-bad `complete` запрещён, а `failed` означает отсутствие какого-либо structurally-valid PID-map после automatic retries.

2026-07-30. Повторная сверка с v4-инвариантами и литературным обзором уточняет статус batch-first: это предпочтительный экспериментальный operational candidate, но не доказанный эквивалент strict exact-RU left-context и не production default до non-inferiority benchmark против strict oracle на frozen boundary golden set (semantic residual, Russian discourse rubric, LTCR и integrity). Phase 3 сохраняет assembled-chapter полноту серией перекрывающихся окон: каждый chunk проходит как full central, каждая boundary наблюдаема, а deterministic checks идут по всей главе; prompt из трёх полных chunk допускается только как risk escalation. `accepted_degraded` — terminal state доступности автоматического выпуска с debt trace, не canonical quality acceptance, не `complete` и не источник authoritative memory.

2026-07-29. Несовпадение production HEAD с commit/tag в deployment provenance — release drift, а не допустимый промежуточный state: active tree и runs сохраняются, reconciliation проходит через reviewed release path. Незавершённый V4 не включается в V3 deployment tree.

2026-07-28. После последнего разрешённого ensemble repair round непринятые кандидаты больше не останавливают pipeline: сохраняется текущий перевод, а lifecycle получает явный терминальный статус `resolved_retry_exhausted`; причина остаётся видимой в quality-артефактах.

2026-07-28. Translation resume may be declared `REUSED` before model startup only when every chunk in each current manifest has a draft cache containing all of that chunk's PIDs; this replaces the deliberately pessimistic always-`MODEL_REQUIRED` probe without trusting a single aggregate output file.

2026-07-28. V4 identity считается проверенной только после содержательной привязки к ожидаемым source/snapshot/authoritative chunk plan/config artifacts; один лишь SHA-256-shaped regex не отвергает foreign identity и недостаточен для cache/resume.

2026-07-28. Phase 0C baseline разделена на два независимых источника, не смешиваемых в одной метрике: Track A — chapter 046 / golden set (FP-candidate rate поверх 57 accepted PID), Track B — глава 100 / v3.1 production run (внутренние bad-repair/residual/integrity/time-tokens). needs_review записи (43) исключены из численных метрик Track A как ограничение неполной курации, не дизайн; semantic recall в этом раунде не измерим (known_violations пустые во всех 100 записях), суррогат не подставляется. Из вывода v3 главы 100 golden-record не строится (schema требует независимый human_translation_epub, которого для главы 100 нет; anchoring bias). Результат прогонов/тексты не коммитятся — только агрегированный versioned result record с хешами/метриками.

2026-07-27. Полная сверка веток репозитория. Все 40+ веток и 46 worktree проверены на уникальное содержимое относительно main — уникального нет. main (тег v3.1.3-hotfix.2) содержит полный состав v3.1.3: atomic artifacts, cache identity, artifact DAG, stage execution protocol, chapter manifest, chapter resolver, formatting integrity. Ветки заархивированы тегами archive/\*, рабочие копии сняты.

B7 инварианты (выявлены по review PR #126):
- `integrity.status` описывает детальное check-level состояние (narrator_gender, formatting, mixed_script, ...), `step8.status` — терминал главы (complete/accepted_degraded/failed/skipped). Они НЕ одно и то же: narrator_gender failure → `integrity.status="failed"`, `step8.status="accepted_degraded"` (PID map валиден, debt trace содержит narrator_gender entry).
- narrator_gender check downgrades только `complete` → `accepted_degraded`; failed/skipped/already-non-complete step8 не трогается.
- `complete` + quarantined chunks = конфликт (assert в `v4_book_run.run_book`).
- bible_text `м`/`ж` одиночные буквы принимаются только из settings-файла (документировано в `extract_narrator_gender`); не из transliterated text.
- Book-run wrapper читает terminal status из `strict_chapter_trial_record.json` (не из argv/return value), quarantined — из `selection_results.json`; strict_main возвращает int.

Тесты: 41 в `tests/pact_v4/test_b7_bible_and_cross_chapter.py` (bible render + truncation suffix + narrator_gender + regex typo guard + prompt rendering + memory promotion + book-run wrapper happy/sad path + bible plumbing), все существующие зелёные.

Хронология 26.07: фичи v3.1.3 вливались поштучно (b513767, da5d09a), откачены в 20:44 (d2c6b36, 2c44679), затем влиты целиком релизным squash-коммитом 231af93f. Откаты — не инцидент, а перестроение способа вливания.

Аудиты V313\_INDEPENDENT\_AUDIT\_FINDINGS.md и V313\_RC2\_DELTA\_REAUDIT.md писались против develop/v3.1.3, а не против main. К текущему коду применимы частично — требуют перепроверки перед использованием как списка задач.

Явно не сделано в этом PR (`codex/v4-phase-2a-risk-prescreen`): на этой ветке нет `pact_v4/phase2/cascade.py` и вообще никакого deterministic-consistency-gate модуля для V4 — Phase 2C (cascaded selection) ещё не начат в `pact_v4/*`. Тест "deterministic gate возвращает not_clean при unresolved required категории" из карточки Work 2 относится к Phase 2C и будет закрыт отдельным PR на `vk/e38a-v4-phase-2c-rebased`, который должен потреблять `REQUIRED_RISK_CATEGORIES` из `pact_v4/phase2/risk.py`, а не переобъявлять список категорий самостоятельно.

Но код `run_chapter_audit` сейчас написан с циклом `for chunk in chunk_plan.chunks: for detector in (qwen, gemma): ...` — то есть per-chunk interleaved, тем же паттерном, что и Phase 2. Если обернуть его в single-resident lifecycle как есть, получится та же цена ~2N, что сводит на нет всё преимущество независимости chunk'ов. Перед тем как Phase 3 будет интегрирована в single-resident driver, порядок циклов нужно поменять на `for detector in (qwen, gemma): for chunk in chunk_plan.chunks: ...` (батч по детектору снаружи, chunk — внутри) — это чисто вопрос порядка итерации, не меняет ни один из чеков (`QwenAuditEvaluator`/`GemmaAuditEvaluator`/deterministic), корректность не страдает, поскольку unit'ы уже резюмируемы и кэшируются независимо (`AuditCache`, per `(chunk_id, detector)` unit_hash). Не сделано в рамках текущей сессии — Phase 3 ещё не подключена ни к одному `pipeline/`-драйверу (см. запись выше про то, что Phase 3 не требует переписывания под strict-vs-batch-first — но эта конкретная оптимизация порядка нужна отдельно, до/во время такой интеграции, а не связана с выбором топологии Phase 1-2).