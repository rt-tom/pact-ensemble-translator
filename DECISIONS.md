2026-07-27. Полная сверка веток репозитория. Все 40+ веток и 46 worktree проверены на уникальное содержимое относительно main — уникального нет. main (тег v3.1.3-hotfix.2) содержит полный состав v3.1.3: atomic artifacts, cache identity, artifact DAG, stage execution protocol, chapter manifest, chapter resolver, formatting integrity. Ветки заархивированы тегами archive/\*, рабочие копии сняты.



Хронология 26.07: фичи v3.1.3 вливались поштучно (b513767, da5d09a), откачены в 20:44 (d2c6b36, 2c44679), затем влиты целиком релизным squash-коммитом 231af93f. Откаты — не инцидент, а перестроение способа вливания.



Аудиты V313\_INDEPENDENT\_AUDIT\_FINDINGS.md и V313\_RC2\_DELTA\_REAUDIT.md писались против develop/v3.1.3, а не против main. К текущему коду применимы частично — требуют перепроверки перед использованием как списка задач.

2026-07-28. V4 identity считается проверенной только после содержательной привязки к ожидаемым source/snapshot/authoritative chunk plan/config artifacts; один лишь SHA-256-shaped regex не отвергает foreign identity и недостаточен для cache/resume.



2026-07-30. `number_word` и `tone_profanity` зафиксированы как required risk/gate категории для V4 (Phase 0C Gate policy, docs/plans/V4_PHASE_0C_GATE_NOTE_RU.md §2). Source of truth — `pact_v4/phase2/risk.py`: `REQUIRED_RISK_CATEGORIES = frozenset({"number_word", "tone_profanity"})`, и `RiskPolicy.__post_init__` отказывается конструироваться, если хотя бы одна из этих категорий отсутствует в `weights` — так требование проверяется на уровне типа, а не только соглашением. Обе категории детектируются source-only (Phase 2A не видит кандидатов/target): `number_word` — written-out cardinal number words в английском источнике (по аналогии с v3's `missing_written_number_words`, но не импортирует из v3 production кода — Phase 2A самодостаточен); `tone_profanity` — источник содержит сильную ненормативную лексику. v3's `pact_translate_v3.py` реализует эквивалентные категории как target-vs-source сравнение (post-generation deterministic gate); Phase 2A pre-screen делает только presence-in-source триггер, поскольку target ещё не существует на этой стадии.

Явно не сделано в этом PR (`codex/v4-phase-2a-risk-prescreen`): на этой ветке нет `pact_v4/phase2/cascade.py` и вообще никакого deterministic-consistency-gate модуля для V4 — Phase 2C (cascaded selection) ещё не начат в `pact_v4/*`. Тест "deterministic gate возвращает not_clean при unresolved required категории" из карточки Work 2 относится к Phase 2C и будет закрыт отдельным PR на `vk/e38a-v4-phase-2c-rebased`, который должен потреблять `REQUIRED_RISK_CATEGORIES` из `pact_v4/phase2/risk.py`, а не переобъявлять список категорий самостоятельно.

2026-07-30. Phase 2B closure (`feat/v4-phase-2b-ab-generation-rebased`): `pact_v4/phase2/generation.py` and `pact_v4/phase2/prompts.py` propagate `REQUIRED_RISK_CATEGORIES` (imported from `pact_v4/phase2/risk.py`, never redeclared) from the source risk pre-screen into the actual generation prompt. `PromptBundle` gained a `required_risk_feature_codes: Tuple[str, ...]` field — the intersection of the chunk's `RiskAssessment.features` codes with `REQUIRED_RISK_CATEGORIES`, computed in `generation.py` and validated (subset check) in `PromptBundle.__post_init__`; it is part of the hashed bundle identity (`artifact: pact-v4-prompt-bundle/v3`), so a chunk whose flagged required categories differ is a cache miss. `prompts.py` holds `_REQUIRED_CATEGORY_INSTRUCTIONS`, a version-controlled mapping from each required category to an explicit instruction string ("Preserve written-out numbers exactly..." for `number_word`, "Preserve source profanity/tone exactly..." for `tone_profanity`); a module-load-time assertion keeps its key set in sync with `risk.REQUIRED_RISK_CATEGORIES`. `render_prompt` appends a `REQUIRED_CATEGORY_INSTRUCTIONS` block to the rendered request only when at least one required category was actually flagged for that chunk — propagation is conditional on the real pre-screen result, not unconditional, and a plain digit `numbers` feature (not the written-out `number_word` category) never triggers it. Temperature, seed, model, and `GenerationParams.reasoning=0` are unchanged from Phase 1A/2A defaults; no v3 production code (`pact_translate_v3.py` or otherwise) was touched. Phase 2C (`vk/e38a-v4-phase-2c-rebased`) is expected to consume `REQUIRED_RISK_CATEGORIES` from `risk.py` the same way, not redeclare it.
